# Backlog

Open work. The Product Owner owns the items — adding, wording, IDs, and the order they sit in. The Team Principal owns only the **Status** column. Items move to `DECISIONS.md` when settled.

Status: `OPEN` · `IN CYCLE` · `NEEDS NATHAN` · `DONE`

---

## Cycle 024 follow-ups (filed 2026-08-24, WP-I bookkeeping)

Every deferred follow-up recorded across cycle 024's work packages in the coordinator's running
tracker, plus this session's own late findings. Filed here rather than dropped, per standing
instruction — including items the tracker itself judged trivial or "no action needed"; those are
marked as such in the Status column instead of being omitted. Owner roles are the Product Owner's
best inference from the item's territory (WP-I does not own backlog content, only status — the
Product Owner should re-file/re-word any of these on the next pass).

| ID | Item | Owner role | Status |
|---|---|---|---|
| B-70 | (Phase 0) Day-mode MapLibre remount fix is narrower than its stated failure class — other whole-style prop swaps (`patchedStyles` null↔JSON, `labelsOff`↔`labelsOn`) still bypass the `key={styleUrl}` fix | Mobile Dev | OPEN |
| B-71 | (Phase 0) `key={styleUrl}` remount discards camera state in free-mode pan/zoom on a theme flip (low frequency, manual toggle only) | Mobile Dev | OPEN — cosmetic |
| B-72 | (Phase 0) `prevEle`/`prevEleTUnixMs` advance even when `appendFix` throws — a diagnostic could reference a fix never written to JSONL | Backend Dev | OPEN — cosmetic |
| B-73 | (Phase 0) No test coverage of `location/index.ts` wiring itself (headless-untestable, expo import) — consider extracting a pure sibling like `elevationOutlier.ts` | QA | OPEN |
| B-74 | (Phase 0) Issue-1 day-mode fix still needs an on-device retest to confirm the diagnosis | Nathan | NEEDS NATHAN |
| B-75 | (Phase 0) `POOR_ACCURACY_M = 50` still uncalibrated (functionally safe, diagnostics-only) — calibrate once enough real route-match-diagnostic events accumulate | Race Engineer | OPEN |
| B-76 | (WP-H) `RUN_DATE` hardcoded to 2026-08-22 in `10_gatefield_replay.py` — should derive from `datetime.date.today()` | Backend Dev | OPEN — trivial |
| B-77 | (WP-H) The gate-field report's std (43.86s) reads as alarming out of context — 486/488 crossings agree within 0.21s; the whole std comes from 2 outlier re-pass/loop cases. Add one sentence naming them | Race Engineer | OPEN — trivial |
| B-78 | (WP-H) Sanity-anchor misses (5/64 Morning) all share one cause (trace starts past the START gate; route model tolerates it, gate-field model doesn't) — worth a sentence in the report plus a cross-ref, decision-relevant for "is gate-field a good idea" | Race Engineer | OPEN |
| B-79 | (WP-H) `scripts/gatefield-replay.cmd` has no `pause`/`@echo off`/`cd /d %~dp0` — the console vanishes on double-click before Nathan can read the output paths or a Python-missing message | Mobile Dev | OPEN |
| B-80 | (WP-H) 3 gate-cluster pairs sit at 33–41 m / <3° bearing diff and would merge under a looser dedup radius (notably StationWorkAlt:G3/StationWorkStd:G3, 41.3 m) — no cached rides on those routes today so no live effect, but latent | Race Engineer | OPEN — latent |
| B-81 | (WP-H) PowerShell nits: unapproved verb, PS5.1 native-call/`2>&1` edge case — both safe | Mobile Dev | OPEN — trivial, no action recommended |
| B-82 | (WP-J pass 1) Dormant separator " · " spacing is off by one space | Designer | OPEN — cosmetic |
| B-83 | (WP-J pass 1) A code comment's date is off by ~22h (zero functional impact) | Designer | OPEN — trivial |
| B-84 | (WP-C) Slider readout (workbench §3b.6) is missing the "+ Google Maps link" the brief required — a workaround exists (name the point), but the one-click link isn't there | Race Engineer | OPEN |
| B-85 | (WP-C) "Propose as reference" checkbox silently drops if ticked before the GPX loads (`promotionState` built fresh on file load, doesn't read the checkbox) | Backend Dev | OPEN |
| B-86 | (WP-C) `reverse` toggled after load leaves `firstPointTime`/`lastPointTime` inconsistent (order-dependent) — affects identity verification before `build_track_ref.ts` | Backend Dev | OPEN |
| B-87 | (WP-C) Arming-window dashed line drawn as a straight 2-point chord instead of "along the route" per brief §3b.2 — visibly cuts corners on curved routes; the page already has the polyline data to interpolate | Designer | OPEN |
| B-88 | (WP-C) `landmarksForWay` parses the wayId string split on `>` instead of the catalog's `startLandmarkId`/`endLandmarkId` — works for all 13 current ways, fragile if an id ever contains `>` | Backend Dev | OPEN — latent |
| B-89 | (WP-C) ~3% stray cloud points inherited from `02_analysis.py`'s `cross_time` behaviour on rides whose first fix is already past a gate, concentrated on START gates | Race Engineer | OPEN — trivial, no action recommended |
| B-90 | (WP-D1) `LiveEngine`: expose `reacquired: boolean` on `LiveFix`, discount ALL re-acquisition hops regardless of magnitude, closing the residual ≤240 m hole in `REACQ_JUMP_M` — a one-line change in `core/src/live.ts` plus a field, per the inspector | Race Engineer | OPEN |
| B-91 | (WP-D1) `live_suite.ts`'s generic per-fixture margin-assertion TRACKS list only loops 3 tracks, not `TRACK_IDS`' 4 — this blind spot is why the original re-acquisition bug was latent; widen it | QA | OPEN |
| B-92 | (WP-D1) `routes.json`'s inverse transform uses `R = 6378137` vs `geo.ts`'s `M_PER_DEG_LAT = 110540` — ~0.7% N–S stretch, drawn-map display only, no effect on chainage/gates/timing; a future pass should convert every entry to 110540 together | Backend Dev | OPEN — cosmetic |
| B-93 | (WP-D1) `gateIdx` is floored, not rounded — the FINISH tick draws ~40 m off true position on MorningB (drawn-map only) | Backend Dev | OPEN — cosmetic |
| B-94 | (WP-D1) `data/analysis/` should adopt the resampled-line metric so future "expected chainage" figures quoted in briefs are measured on the line the engine actually projects onto (root cause of the WP-D1 MorningB escalation) | Race Engineer | OPEN |
| B-95 | (WP-D2) N1: Morning/StationWorkStd/WorkFosh (a close 200 m-margin rival pair) no longer soft-lock live without a pick — only resolve at `finalize()`. Pre-resolved by the brief but worth recording since it changes what the live screen shows mid-ride on these routes | Race Engineer | OPEN — informational |
| B-96 | (WP-D2) N2: soft→verified promotion emits no second lock event — a persisted sidecar can read `lockKind:'soft'` for a ride that ended verified (transparency gap only) | Backend Dev | OPEN |
| B-97 | (WP-D2) N3: `finalize()`'s winner tiebreak (max adv + pick) ignores `anchored`, asymmetric vs the live rule's anchored-first logic — no real-catalog case found yet, but worth closing | Race Engineer | OPEN |
| B-98 | (WP-D2) N4: once a soft-locked candidate's own FINISH fires, the `phase !== 'finished'` guard freezes live evaluation for the rest of the ride — the end result is correct (B1 fixed it) but a rider watching mid-ride still sees the frozen prefix route until they stop. Possible future work package for live-screen correctness, not just end-of-ride | Race Engineer + Mobile Dev | OPEN |
| B-99 | (WP-D2) N5: the WP-D1 adjudication's `REACQ_JUMP_M = 240` reference is now stale (actual value is 245, from a later coordinator chore) — doc-only | Race Engineer | OPEN — trivial |
| B-100 | (WP-D3) Test 3 is a "mirror implementation" test by the brief's own design, not hollow — recorded so it isn't re-flagged as coverage-theatre | QA | OPEN — no action needed |
| B-101 | (WP-D3) A doc comment says "catalog order breaks ties" but actually first-in-results-array wins on exact-equal timestamps — comment-precision only, unrealistic edge case | Backend Dev | OPEN — trivial |
| B-102 | (WP-D3) CRLF/LF cosmetic difference, zero git-diff impact per `.gitattributes` | Mobile Dev | OPEN — trivial, no action needed |
| B-103 | (WP-A1) `LiveEngine.finalize()`'s finished predicate can also be satisfied by a candidate whose gates were all SKIPPED (zero real fires) via late-lock arming — can only produce a fully-missed unrankable lap, but is eligible to become the DISPLAYED route name. Worth a look next time `engine.ts` is open | Race Engineer | OPEN |
| B-104 | (WP-A1) `app/tests/results_cache_suite.ts` is a comment-only stub (sandbox blocked a real `mv`; content preserved at `safe_to_delete/results_cache_suite.ts`) — a real move would tidy this once tooling allows it; also a stray `safe_to_delete/_rmtest.txt` probe file | Mobile Dev | OPEN — housekeeping |
| B-105 | (WP-A1) `pushRecorded` still hardcodes `lap.quality:'clean'` in-session (pre-existing from B-40, cosmetic, verified no unrankable ride can leak through) | Backend Dev | OPEN — trivial |
| B-106 | (WP-A1) `isStale()` is never consulted by the new results store (B-20's territory, out of scope for WP-A1) | Backend Dev | OPEN |
| B-107 | (WP-A1) A valid-but-stale `index.json` (process killed between ride-file write and index write) self-heals via backfill re-derivation but with a different `engineVersion` tag | Backend Dev | OPEN — trivial |
| B-108 | (WP-A2) BackHandler comment's ordering rationale is factually inverted (claims RecordScreen's handler runs before Shell's; actually Shell's runs first via LIFO dispatch) — behaviour is correct only because Shell's handler no-ops on the record tab; misleading for future edits | Mobile Dev | OPEN |
| B-109 | (WP-A2) `refreshPositionOnce()` can land after `startTracking()` clears display state if GPS is cold and the rider presses START within ~1–2s of arming — needs a `session === null` guard | Mobile Dev | OPEN |
| B-110 | (WP-A2) Double-END after a failed `stopTracking()` can duplicate a lap in the ghost/comparison window (`pushRecorded` unguarded, `ghostsFor` doesn't dedupe) — skews mean/sd and therefore the colour tier of the NEXT ride (D-025/D-028-adjacent). Needs a 3-line `endedRef` guard in the onEnd catch | Mobile Dev | OPEN |
| B-111 | (WP-A2) `ending` phase swallows hardware back; if `reduceMotion` never resolves, the reverse overlay can hang with only tap-to-skip as an out — needs a watchdog timeout | Mobile Dev | OPEN |
| B-112 | (WP-A2) `canTransition()`'s state table has zero production callers (RecordScreen sets phase directly throughout) — per brief design, not a deviation, but "197/205/239 green" should not be read as coverage of the actual live state machine | QA | OPEN — no action needed |
| B-113 | (Footer-overlap hotfix, 2026-08-24) New padding strip paints `t.bg` not `t.race.bg` — a slightly different shade band in both themes, may read as intentional footer or as a new cosmetic bug | Designer | OPEN |
| B-114 | (Footer-overlap hotfix) The forward launch-mark overlay, which used to cover edge-to-edge, now also stops short of the true edge by `bottomPad` | Designer | OPEN — cosmetic |
| B-115 | (Footer-overlap hotfix) 12dp floor means "fullscreen" is never literally edge-to-edge even at `insets.bottom = 0` (pre-existing, not new) | Mobile Dev | OPEN — trivial |
| B-116 | (Footer-overlap hotfix) Confirm the reported overlap was the real OS nav bar (gesture/3-button), not Expo preview-harness chrome — if the latter, `useSafeAreaInsets()` won't see it and the fix only gets partial credit | Nathan | NEEDS NATHAN |
| B-117 | (WP-A3) Both RIDES and RESULT gate ranking on `movingS !== null` rather than the store's own `ranks()`, which also excludes `tripwireDemoted` — exactly the "local lookalike of `ranks()`" `colourModel.ts` warns against by name. Not reachable today (nothing sets `tripwireDemoted` on app rides yet) but will silently rank+colour a demoted lap the moment D-024's tripwire is ratified for app rides | Mobile Dev | OPEN |
| B-118 | (WP-A3) The brief said "keep" the gate-colours memoization; `ResultScreen` ships it unmemoized (`resultGateColours`, `pbRows`, `routeIdsInHistory` all recompute every render) — harmless at today's data volume, cleanliness only | Mobile Dev | OPEN — cosmetic |
| B-119 | (WP-A3) `buildPbDetail` shows a literal "today" for the `lastRideId` row via `getLastRideOrStored`'s stored-fallback, which after time away is not actually today — the one relative label on an otherwise-all-absolute-dates screen (brief-specified, not the executor's invention) | Designer | OPEN |
| B-120 | (WP-A3) `RidesScreen` bumps a tick to re-read `getStoredResult` after backfill but doesn't merge new results into the `recorded` array `lapValues`/`ghostsFor` actually read (theoretical — boot's own `initRideHistory` pass normally wins the race) | Mobile Dev | OPEN — latent |
| B-121 | (WP-A3) `app/tests/results_cache_suite.ts` (the WP-A1-era stub) is still on disk, not imported by `run.ts` since WP-A1 — dead file, safe to delete | QA | OPEN — housekeeping |
| B-122 | (WP-G) Silent no-`refFor` degrade has no signal in the GPX+ export (indistinguishable from "never locked") — consider an explicit `<qf:routeFidelity>unavailable</qf:routeFidelity>` marker | Backend Dev | OPEN |
| B-123 | (WP-G) `Candidate.lastXtd` inits/resets to sentinel 999 (`number`, not `number\|null`) — safe today only because of current emit ordering; also collides semantically with `routeFidelity`'s own 999 display cap | Backend Dev | OPEN — latent |
| B-124 | (WP-G) Decoder validation test now covers 5 of 8 event kinds (lock/gate/button/meta + the shared time bound via `elevationOutlier`) — extend to `storageError`/`relaunch`/`routeMatchDiagnostic` explicitly | QA | OPEN |
| B-125 | (WP-G) `maxXtdM`'s 999 cap is unmarked as a cap — a genuinely 5 km-off ride reads "999.0" as if it were a real measurement | Backend Dev | OPEN |
| B-126 | (WP-G) Part 3 elevation flags are record-time only — historical rides (including the ones that originally motivated the ask) can never be flagged retroactively; the brief's original export-time design would have covered them | Backend Dev | OPEN |
| B-127 | (WP-G) `xtdM`'s 2 `live_suite.ts` test additions are shallow (typeof-only, would pass on the old 999 sentinel too) — sourcing was verified correct by the inspector via code trace instead; consider strengthening to compare against an independent `LiveProjector` computation | QA | OPEN |
| B-128 | (WP-B) `mode` lives only in `index.json`, not recoverable from the raw JSONL header if the index is ever lost/corrupted — `rebuildIndex()` would silently forget every past free ride's mode, making them backfill-eligible again. Add `mode` to `encodeHeader`'s ride-file header record too (D-023-compatible, written once at creation) | Backend Dev | OPEN |
| B-129 | (WP-B) On a genuine recovery (not just reopening the app), `RecordScreen` never restores `rideFreeRouteIds` — the gates-only map shows all routes rather than the ride's original filtered set after a true relaunch (cosmetic; the engine candidate restriction itself is correct via the persisted `routeIds`) | Mobile Dev | OPEN — cosmetic |
| B-130 | (WP-B) Pending-sectors strip renders meaninglessly above the free-sector list in free mode | Designer | OPEN |
| B-131 | (WP-B) `freeRideRouteIds` returns `[]` (not `null`) for a landmark with no ways in that direction — currently unreachable, only affects the dormant/inactive puttestraat landmark | Backend Dev | OPEN — latent |
| B-132 | (WP-B) `free-rides-cache.json` is one unbounded flat file with no pruning (unlike `resultsStore`'s per-ride files) | Backend Dev | OPEN |
| B-133 | (WP-B) `isValidFreeRideRecord` never checks `schemaVersion` | Backend Dev | OPEN |
| B-134 | (WP-B) A scratch replay test file `app/tests/scratch_freeride_replay.ts` was left in the tree as an inert stub, not imported by `run.ts` — decide keep or delete | QA | OPEN — housekeeping |
| B-135 | (WP-B) `store_suite.ts`'s `freeRideRouteIds` test runs against a synthetic catalog rather than the real seed (hand-verified correct against the real seed by the round-1 inspector, but no regression test pins it) | QA | OPEN |
| B-136 | (WP-B) A free ride's GPX+ export interleaves gate events from multiple tracks under `routeLock=none` with nothing marking it as a free ride specifically — would be closed for free by B-128's header-level mode marker | Backend Dev | OPEN |
| B-137 | (WP-B) One test title in `live_suite.ts` overclaims what its own assertion proves (the underlying code is correct, verified independently by direct code trace) | QA | OPEN — trivial |
| B-138 | (WP-B) GPX+ relaunch-count fidelity note: the relaunch diagnostic now also fires on the "RecordScreen discovers a dead service, offers Save ride" path, which it technically didn't before — makes `<qf:relaunches>` counts occasionally higher but more accurate, not a regression | Backend Dev | OPEN — informational |
| B-139 | (WP-B, Nathan's own explicit deferral) The "new>>new" free-ride design task — both start and end unknown at once. Nathan deferred this by name during WP-B's dispatch (task #18); both-ends-"new" currently stays unfiltered on the gates-only map/engine candidates rather than being designed for. Needs a future design pass before it's ratified | Product Owner + Designer | OPEN |
| B-140 | (WP-E) Extend the gate-tick rendering to WP-B's gates-only multi-route map — a ~25-line mirror of `gateTicksFeatureCollection` using the per-route asset already available in `allGatesFeatureCollection`'s loop. The free-ride map still has the pre-cycle-023-fix circle-visibility bug live (transparent-fill, dark-stroke circles, nearly invisible at night) — also correct the `routeMapView.tsx:328-332` comment's wrong "doesn't generalize" justification when this is picked up | Mobile Dev | OPEN |
| B-141 | (WP-E) N3: prestart counts as `active` in `routeSplitFeatures`, so the pre-start whole-route preview is now entirely dotted (defensible — "all of it is ahead" — but worth a Nathan eyeball, since prestart is otherwise treated as browse-like elsewhere in this component) | Nathan | NEEDS NATHAN |
| B-142 | (WP-E) N4, investigated and refuted as a real concern — recorded so it isn't re-investigated: self-approaching route geometry could in theory make the dotted/solid split boundary ambiguous near a coincident vertex; checked all 20 routes, genuine minimum self-approach is 96 m, requiring ~48 m of off-path to flip the boundary. `offRouteM` itself is immune regardless | Race Engineer | OPEN — no action needed |
| B-143 | (WP-E) N5: PNG-rung tick length has a min-10px floor that biases it slightly off-centre at zoom 1 — cosmetic, exactly as the brief prescribed | Designer | OPEN — trivial |
| B-144 | (WP-E) N6: MapLibre source id and layer id both literally "gate-ticks" — legal (separate namespaces) but confusing to read | Mobile Dev | OPEN — trivial |
| B-145 | (WP-E) N7: `riderBlue #2F7DE1` is unratified and sits close to the night dark-basemap's water-blue near the Leuven canal — worth a look on-device | Nathan | NEEDS NATHAN |
| B-146 | (WP-E) The device-visual check itself (both themes, both rungs) is explicitly Nathan's own per the brief, not done by either agent | Nathan | NEEDS NATHAN |
| B-147 | (Map-rendering hotfix, 2026-08-24) MapLibre gate ticks have no minimum on-screen length at FIT zoom (the PNG rung already clamps to 10px, the MapLibre rung doesn't) — likely source of a future "can't see the gates" report on the browse/Result map | Mobile Dev | OPEN |
| B-148 | (Map-rendering hotfix) PNG rung gate ticks have no casing/outline (same rung-parity gap as before, near-unreachable rung) | Mobile Dev | OPEN — cosmetic |
| B-149 | (WP-J re-emit) The PURPLE_INK-on-bare-text contrast bug (`RidesScreen.tsx` sector rows + `RecordScreen.tsx`'s `gateColours` memo) is a real app bug, confirmed by the Opus reviewer, and was deliberately reproduced (not "corrected") in the `design/` SVGs, consistent with that folder's stated purpose as a mirror of what actually ships. It is a genuine app-code contrast bug and still needs an actual fix in `RidesScreen.tsx`/`RecordScreen.tsx` | Mobile Dev + Designer | OPEN |
| B-150 | (This session, WP-I bookkeeping) `design/canonical/settings_day.svg` and `settings_night.svg` still label the Scoring row "Timing tower" — the real app (`app/src/ui/settings.tsx:173`) renamed this to "Rankings" during WP-A3, before the first-pass SVGs were drawn; the SVGs were never regenerated after that rename. `design/` is frozen for the rest of this cycle (WP-J's territory) — fix at the next `make_screens.py` touch by updating the row label at `design/make_screens.py:863` | Designer | OPEN |
| B-151 | (This session, WP-I bookkeeping) `design/README.md` and `routeMapView.tsx` both confirm MapLibre's gate ticks have no minimum on-screen length at FIT zoom, and the PNG rung has no casing — duplicates B-147/B-148, cross-referenced here only because it was independently reconfirmed by re-reading `design/README.md`'s own drawing-conventions section during this pass | Mobile Dev | OPEN — duplicate of B-147/B-148, no new action |
| B-152 | (This session, WP-I bookkeeping) The original WP-I brief (`cycles/cycle-024-briefs/WP-I-housekeeping-nathan-docs.md` §11) expected `product/DATA-MODEL.md` §8a and `product/COLD-START.md` §10 to carry a 2026-08-20-dated follow-up note recording the lock-then-verify pick-bias ruling before `COLD-START.md` could safely move to `proposals/`. Checked directly this session (`grep -n "2026-08-20" DATA-MODEL.md COLD-START.md`, both files, 2026-08-24): **no such note exists in either file.** `COLD-START.md` was moved to `proposals/` anyway on the (separate, current) grounds that it is genuinely an unbuilt design proposal — but the promised cross-reference to D-044 (this cycle's pick-bias ruling) is still missing from both files and should be added | Product Owner | OPEN |

---

## Now — concept definition

| ID | Item | Owner role | Status |
|---|---|---|---|
| B-01 | Settle the colour model: how many tiers, which horizons, rollover behaviour | Race Engineer + Designer | DONE → D-007, D-008 |
| B-02 | Define sector boundaries: fixed-distance vs landmark vs variance-derived | Race Engineer | PART-DONE → D-016: 4 sectors/track proposed from data (`gates_proposal.csv`); awaiting Nathan's map eyeball |
| B-03 | Reference lap semantics: deliberate vs automatic, reset cadence | Product Owner | DONE → D-009 (PROVISIONAL, awaiting Nathan) |
| B-04 | Decide whether to/from work are separate boards | Product Owner | DONE → D-010 |
| B-05 | Confounder policy: stopped time, traffic lights, weather | Race Engineer | OPEN |
| B-06 | Safety stance: post-ride feedback only, or live? | Product Owner | DONE → D-006 (live) |
| B-07 | Sketch the post-ride results screen (the F1 timing board) — superseded by B-29 (board v2) | Designer | SUPERSEDED → B-29 |
| B-15 | Design the **live** in-ride display under D-006's constraints — glanceable, audio/haptic-first, no ticking countdown — being reworked under B-30 (IDEAS §16); reconciliation pending | Designer | RESOLVED → D-027 (reconciliation recorded; live v2 in LAYOUT §2) |
| B-16 | Real-time sector detection: cross a boundary and compute a time *while riding*, on a noisy trace | Race Engineer | CODE COMPLETE headless (cycle 006 → D-025) — awaiting first on-device commute |
| B-17 | Obtain recorded commute traces to measure σ_s and validate D-011 offline | Nathan | DONE — Strava archive received 2026-08-14: 624 GPX files in `strava_export-20260814.zip` |
| B-18 | Prior-art survey: Strava segments mechanic, OpenTracks, Open Pace; and building blocks (gpxpy, leaflet-gpx, map-matching libs) for an offline trace-replay validation tool | Race Engineer + Mobile Dev | OPEN |
| B-19 | Phase-0 validation harness (per D-012): parse the Strava archive, auto-detect commute rides by start/end clustering, measure per-sector variance σ_s, replay D-011 sectoring | Race Engineer + Mobile Dev | OPEN |
| B-20 | Define sector-of-the-day metric and gate-move history-invalidation semantics (exported from LAYOUT.md) | Race Engineer | OPEN |
| B-21 | Comparability study: Strava-archive vs app-recorded traces | Race Engineer | DONE → D-018 resolved: pre-seed with guards |
| B-22 | Redesign reference-ride semantics as a deliberate event per D-017 | Product Owner | DONE → D-021 "Quali Day" |
| B-23 | Fold D-019 into LAYOUT.md; spec the three tier earcons | Designer | DONE — earcons audible in demos/mockup.html |
| B-24 | Phase-1 wiring: FGS task + trace storage + GPX export | Mobile Dev + Backend Dev | DONE — acceptance passed 2026-08-15 (real export GPX; exposed F-2, fixed + regression-locked same cycle) |
| B-25 | "REFERENCE SET" ceremonial frame + defended/set wording | Designer | DONE — LAYOUT §3a + mockup demo state |
| B-26 | D-022 lap scoring into LAYOUT + mockup | Designer + Race Engineer | DONE — LAYOUT §2a; lap chip + two-part final-gate earcon audible in mockup |
| B-27 | On-bike audibility test: earcon bands vs wind at 25 km/h — now doubly needed (lap voice sits at 330–494 Hz, below the wind-safe band) | Nathan + Designer | OPEN — needs a real ride with sound |
| B-28 | **Timing tower** (IDEAS §15): rank today's lap among the trailing-28-day lap set, per track. Semantics per PO cycle-007 spec: clean + interrupted moving-time laps rank; estimated laps never rank (today's own shows unranked "NO TIME"); P1 = pole of the 28-day session; Quali reference (D-021) carries a REF badge, distinct from P1 — "defending pole" = REF==P1; PB badge dot kept (D-007); archive-seeded laps rank as marked ghosts, demoted to unranked if the D-024 cruise-σ tripwire fires. Builds on the benchmark store | Product Owner + Race Engineer + Backend Dev | DONE 2026-08-17 (cycle 016) — `TimingTower` wired into Result via pure `towerModel.ts` (window rows, dates, ghost ○, PB ●, once-per-ride slot-in); posChip was already live (cycle 008); window = last-10 per D-037, not 28-day; REF badge (D-021) still parked as a taste check |
| B-29 | **Post-run board v2** (IDEAS §17): tower as headline (today's row slots in — the reveal ceremony lives here) → four sector rows → quarantined ideal-lap line (untiered, D-022); "Sector of the day" dropped (Nathan's word); Quali ceremonial frame (B-25) wraps the board only when armed. Position is a fact, colour comes only from the lap tier — bottom-half positions carry no failure styling (D-013). Replaces the delta plot ("instead of plotting the points") | Designer + Product Owner | CODE COMPLETE (cycle 007 — app preview + mockup.html) — device feel pending |
| B-30 | **Live-screen redesign** (IDEAS §16): dominant 0.1 s ticking counter + sector blocks that flash the checkpoint time in the earned colour; final-gate handover additionally shows the tower position as a static chip beside the lap tier, no new earcon. **Conflicts with B-15/D-006 no-ticking rule — the safety reconciliation is part of this item; the Principal adjudicates** (proposed, unratified: audio stays primary on-bike, rich ticking display is the glance/stopped view). Demo must remain on the shared render path (§17 obligation) | Designer | CODE COMPLETE (cycle 007 → D-027; live v2 in app + mockup) — on-device feel Monday |
| B-31 | **Gate-eyeball upgrade to `demos/gates-check.html`** — unblocks B-02, PART-DONE since D-016 "awaiting Nathan's map eyeball". Upgrade the existing Leaflet demo; do **not** create a new file. Per gate it must draw: the gate **line**, perpendicular to the reference polyline at its true half-width (never a dot — a dot hides a line clipping a parallel road or a second roundabout arm); the reference polyline; the cloud of actual crossing points from the 125 cached rides in `data/analysis/cache/*.npz`; the engine's 50 m arming window as an interval; and a label carrying the incoming sector's σ_s plus `median_speed_kmh` / `stop_frac` from `data/analysis/gates_proposal.csv`. Every gate readout carries a **Google Maps link** for its coordinates. Acceptance: Nathan opens the file and can say "move gate 2", one gate at a time. Zero build, independent of Monday's commute | Race Engineer + Designer | OPEN |
| B-32 | **Basemap ground: light vs dark** — under D-041 this is a style-URL swap (OpenFreeMap `positron` vs `dark`, both verified 2026-08-17), not custom rendering. Two questions, not one: the Designer rules **dark** for the live ribbon (one ground colour with the race-mode screen); the browsing surfaces (Routes, Result view-trace, gate-setup) may prefer the light wash. Acceptance: Nathan looks at one render of each, told which surface each is for | Designer + Nathan | OPEN — Nathan's eye, after B-50 |
| B-33 | **OSM traffic-signal chainages, extracted offline** — one CSV per route in `data/analysis/`, from Overpass or a Geofabrik extract, **not** from the rendered tiles (which carry no topology and no stable way IDs). Feeds B-05 (confounder policy): a stop at a known signal chainage is luck, a stop elsewhere is not. Also makes "gates go tens of metres downstream of the junction exit" computable rather than eyeballed, serving IDEAS §22. Honest caveat: 624 rides of stop locations would reveal the same signals eventually — OSM only names them sooner | Race Engineer | OPEN |
| B-34 | **Live-ride map overlay** — was NON-GOAL under D-033; **reopened by Nathan 2026-08-17 (D-041)**: the map goes on every screen incl. live, under D-033's reserve rules. Superseded by B-50/B-51 | Product Owner | SUPERSEDED → D-041, B-50/B-51 |
| B-35 | Cold-start ladder: verdict-free ride-1 board + the "ride n of 5" progress line + the two one-time announcements at n=5 and n=10 (§28). Ride 1 shows times and within-ride shape, never a tier, never a rank — "P1 of 1" is a joke, not a fact. The countdown is the ride-1 product: it converts an empty app from *broken* into *loading* | Product Owner + Designer | OPEN |
| B-36 | Retroactive way creation: name start/end at STOP when no landmarks exist; landmarks born from visited endpoints rather than mined from the archive (§28). This is the inversion the cold start forces — on a virgin install setup is retroactive, and §21's START→autodetect→pick-destination flow is archive-dependent end to end | Mobile Dev + Backend Dev | OPEN |
| B-37 | Provisional gates from 2 matched traces (equal-chainage split), superseded by measured gates once history allows (§28, §22). Overlaps the geometric gate proposal in B-46's routing design — the Race Engineer owns one algorithm, not two | Race Engineer + Backend Dev | OPEN |
| B-38 | Sector count scales with a way's length/duration instead of a fixed four. Four sectors over a 4-minute errand are 60 s blocks dominated by GPS noise; over a 2-hour ride they are useless | Race Engineer | OPEN |
| B-39 | De-hardcode route identity: `FALLBACK_ROUTE`, literal route IDs in `ResultScreen.tsx` and the shipped `results.seed.json` become data; empty-seed install path. Blocks every user-created way | Mobile Dev + Backend Dev | PART-DONE 2026-08-24 (cycle 024, WP-D3) — `defaultRoute.ts` built; `ResultScreen.tsx`/`RecordScreen.tsx`/`routeMapView.tsx`/`DemoScreen.tsx` de-hardcoded (grep gate: exactly 1 literal left, the intentionally-exempted `DemoScreen.tsx`). The empty-seed install path is still untouched — remains OPEN |
| B-40 | Persist the comparison window across app restarts — fold into the benchmark-store item. `recordedResults()` is memory-only (`lastRide.ts`), so the window resets on every restart: Nathan already lives a miniature cold start daily. Cold-start is a second, independent argument for the store | Backend Dev | CODE COMPLETE, uncommitted (cycle 015) — `lastRide.ts` persists `recorded` to `results-cache.json` via the existing FsAdapter, rehydrated once at boot in `App.tsx`; 4 new regression tests (`tests/results_cache_suite.ts`), suite 108: 105 pass / 0 fail / 3 skip, `tsc` clean, independently inspected. **Not committed**: working tree is on `spike/maplibre` with unrelated uncommitted map WIP sharing `tests/run.ts` — see cycle-015 for why a commit was deferred rather than guessed at |
| B-41 | Alternatives: multiple ways sharing one endpoint pair, auto-matched at route lock rather than pre-declared, grouped but never colour-compared (§28, D-010/D-015). The rider should not have to declare which road they will take | Backend Dev + Product Owner | OPEN |
| B-42 | Reference traject pick: ride 1 is the reference by default; promote any clean complete lap afterwards (§28) | Product Owner + Mobile Dev | OPEN |
| B-43 | Empty-state pass: "0 rides found", no route lock on ride 1, tower below MIN_HISTORY, and an explanation for the sector-grey-while-lap-coloured asymmetry (clean-only sector history vs any-quality lap history — correct per D-008, reads as a bug on a new way) | Designer | OPEN |
| B-44 | **BUG, verified in code by the Principal this cycle: today's own lap sits inside its own comparison history.** `ghostsFor()` = `[...GHOSTS, ...recordedResults()]` and `rememberRide()` calls `pushRecorded(last)`, so by the time `ResultScreen.tsx:53` sets `others = laps` for a real finished ride, today's lap is already in `laps`. Consequences: `value < st.best` can never be true, so **a personal best can never render purple on the Result screen**; `positionAmong` prepends the value to a history that already contains it, so 10 rides read "P1 of 11"; and MIN_HISTORY is reached one ride early. Acceptance: a regression test that finishes a ride faster than every ghost and asserts purple + correct field size | QA + Mobile Dev | DONE — fixed 2026-08-17 (cycle 013): `ghostsFor(routeId, excludeRideId?)` excludes today's `session:` id before the window slice; regression tests in `tests/live_colour_suite.ts` (pre-fix FAIL verified; suite 96: 93 pass / 0 fail / 3 skip; independently inspected) |
| B-45 | Reconcile D-028's 28-day tower window with `WINDOW_N = 10` | Team Principal + Product Owner | DONE → D-037 |
| B-46 | **MapLibre install spike, costing zero EAS builds** — branch, `expo install`, plugin entry, local `expo prebuild --clean --platform android`, `tsc --noEmit`, `expo export --platform android`, read the generated manifest. Settles the only two things reading cannot: resolution under this repo's Metro conventions, and whether the native build links. Condition 1 of D-035/D-041. **Ungated by D-041** — runs now, as `scripts/spike-maplibre.ps1` on Nathan's PC (the cloud sandbox cannot reach npm) | Mobile Dev + Nathan | Mobile Dev + Nathan | DONE 2026-08-17 — PASS on Nathan's PC (prebuild, tsc, export clean; committed on branch `spike/maplibre`, zero EAS builds); B-50 unblocked |
| B-47 | **Battery A/B: PNG vs MapLibre**, two back-to-back commutes, same phone, same brightness, same route, `dumpsys batterystats --reset` before and Battery Historian after; read the difference, not the absolute. Condition 2 of D-035/D-041 — required before the preview APK ships with the map, not before dev-client work; also settles `STATE.md`'s standing battery question | Nathan + Mobile Dev | OPEN — needs two commutes, after B-50 |
| B-48 | **Replay the routing numbers on the 624-ride archive before anyone trusts them.** Overlap tolerance 25 m and minimum shared length 800 m contiguous + one whole sector; segmentation `n = clamp(L/1400, 3, 6)` with a 300 m floor and 2500 m ceiling. All are starting values chosen from measured evidence, none has been on the test bench. Also benchmark stage-3 chainage projection on device (~236k point-projections for 100 survivors, `[UNVERIFIED]`) before a screen is built around it | Navigation Engineer + Race Engineer | OPEN |
| B-49 | **Price a routing native module for Expo.** No maintained React Native / Expo binding exists for BRouter, Valhalla or GraphHopper — every path is custom native work. Also: 74.7 MB of BRouter segment data must be a *downloaded* asset, not bundled, and must survive an update. Offline routing is CPU-bursty at plan time and zero-cost while riding — no background service, no radio, no mid-ride drain | Mobile Dev | OPEN |
| B-50 | **MapLibre + OpenFreeMap vector base replacing `routeMapView.tsx`'s PNG compositor**; route/gates/dot re-implemented as GeoJSON layers per `product/MAP-TILES.md` §5 (v11 declarative API); PNG kept as the failure fallback rung; needs one dev-client rebuild after B-46 passes | Mobile Dev | DONE 2026-08-17 (cycle 015) — code landed on `spike/maplibre`, inspector PASS, 108 tests 0 FAIL, tsc clean; awaiting build 4 (dev-client only, per Nathan 2026-08-17) + on-device acceptance |
| B-51 | **Wire the real map onto every screen per `product/MAP-CONTRACT.md`**: Record's four ride states (pre-start / moving / stopped-at-light / finished), Demo (shared component), Result's new view-trace link (B-57), Routes overview's static-Image swap | Mobile Dev + Designer | DONE 2026-08-17 (cycle 016) — all four states + browse variant + runtime palette firewall/label hiding; inspector PASS; on-device acceptance rides on build 4 |
| B-52 | **Ambient tile cache for no-signal riding** (online-first per Nathan; short of a PMTiles file) — that MapLibre's ambient cache serves previously seen tiles offline is [UNVERIFIED] until seen on device; acceptance item of B-50's first dev-client run | Mobile Dev + Backend Dev | OPEN — blocked on B-50 |
| B-53 | **"Map data sources" attribution sheet** (Strava-style), replacing the in-corner credit string; owed strings in `product/MAP-TILES.md` §6 | Designer + Mobile Dev | DONE 2026-08-17 (cycle 016) — credit tap opens the sources modal, inert while moving/stopped (D-006) |
| B-54 | **Free satellite-layer sourcing spike** — no keyless free source found in cycle 014 (Esri World Imagery needs a key, 2M tiles/mo free); report cost/licence before any satellite work is scheduled | Mobile Dev | OPEN — LATER |
| B-55 | **Layer switcher UI** (streets ⇄ satellite), top-right where the zoom bar sits, never rendered while moving | Designer | OPEN — LATER, blocked on B-54 |
| B-56 | **Protomaps PMTiles offline corridor extract** (`pmtiles extract https://build.protomaps.com/YYYYMMDD.pmtiles leuven-z15.pmtiles --bbox=4.60,50.81,4.72,50.89 --maxzoom=15`, copy to `filesDir` on first run) — only if B-52's ambient cache proves insufficient on a real dead-zone commute | Mobile Dev | OPEN — LATER |
| B-57 | **Result screen "view trace" link** (LAYOUT §3 rule 6) — unbuilt today; worth building now that it opens onto a real map | Designer + Mobile Dev | DONE 2026-08-17 (cycle 016) — VIEW TRACE toggle, route on real streets with today's gate colours; true fix-trace still awaits a JSONL reader (D-023) |
| B-58 | **Phone-checkable acceptance test for "the map problem is solved"** (`product/MAP-CONTRACT.md` §4), folded into `app/README-dev.md`'s acceptance steps | QA + Product Owner | DONE 2026-08-17 (cycle 016) — step 10 with the explicit FAIL clause |
| B-59 | **Red-light timing semantics per D-042** — default `off` = RAW time everywhere (luck counts; colours/ranks/tower/clock stop using movingS), `auto` = stationary-detected pause, `button` = manual pause; comparison sets must be mode-consistent (never rank raw against moving, D-025); re-examine the interrupted-sector exclusion under `off` | Race Engineer + Mobile Dev | OPEN — ruled 2026-08-17 (D-042), deliberately NEXT build cycle, not the current tree |
| B-60 | **Ratify the home→work "rain/asphalt" seed route.** Nathan's ways review (`demos/ways/home--work.html`) tags ride 2026-06-12 10:42 (home→work, 16 min) as `SEED for a new route: the home→work mirror of Evening B (0.71–0.73 with 4/5/10) — the rain/asphalt route`. Per D-015 / DATA-MODEL §8a a route is ratified, not discovered: one sample ride is a proposal, not a track. Work: pull every home→work ride in the 624-ride archive that clusters with this seed (same overlap method as D-015), render them beside Morning and Evening B, and put the cluster in front of Nathan's eye — either it becomes a fourth ratified track (Morning-wet?) with its own gates and boards, or it is recorded as a detour. Until then it stays out of `catalog.json` | Navigation Engineer + Nathan | PART-DONE 2026-08-19 (cycle 020) — Nathan ruled by eye: home>work route B = route 5 (20260520-2317) reversed; now a LIVE track (`MorningB`, 4th engine candidate) with cold-start gates (EveningB's reversed + START ~162 m, unratified). The archive clustering pass is still open |
| B-61 | **Leuven station ↔ work: two named variants plus four untriaged rides** (`demos/ways/station--work.html`, 10 rides). Nathan's notes name `work→station route A` (2026-04-28 18:02) with its reverse `station→work alternative (reverse of 1, 0.75)` (2026-05-01 20:49), and `work→station route B (reverse of 9/10)` (2026-08-08 09:58) as the reverse of the `station→work standard` (2026-08-08 18:41; 2026-08-09 18:05, 0.85 overlap). Four station→work rides are still `unreviewed` with empty notes: 2026-06-07 09:34, 2026-07-17 21:28, 2026-07-20 10:01, 2026-08-03 10:34. Work: (1) triage the four unreviewed rides against A / standard; (2) ask Nathan whether A and B/standard are real alternatives (COLD-START §10: same endpoint pair, grouped, never colour-compared — B-41) or one route with a detour, before either enters `catalog.json` | Navigation Engineer + Nathan | OPEN |
| B-62 | **home ↔ Leuven station: "via-fosh" variant vs "PREFERRED standard", plus one odd ride** (`demos/ways/home--station.html`, 11 rides). Nathan's notes: `via-fosh route` on 2026-05-17 11:48, 2026-06-06 12:36, 2026-07-17 16:16, 2026-08-01 13:05 (all home→station); `PREFERRED standard — shortest (8.74 km), avoids fosh` on 2026-07-21 11:32 (home→station) paired with 2026-07-21 20:48 (station→home, 8.67 km, 0.74); a station→home ride that `passes fosh but on different roads than 4/6/8/11` (2026-05-17 20:50) which `pairs with` 2026-06-21 07:17 (0.85); and one `unreviewed` ride 2026-05-03 21:03 (`odd one out — matches nothing well, best 0.53 with 5`). Work: confirm with Nathan that via-fosh and standard are two ratified alternatives of the same way (B-41 grouping, never colour-compared) rather than a preferred route plus a detour; decide whether the fosh-on-other-roads pair is a third variant or noise; triage the odd ride. Note the "PREFERRED" tag — the first time Nathan has ranked alternatives; DATA-MODEL §8a's START route-pick should default to it | Navigation Engineer + Nathan | OPEN |
| B-63 | **church ↔ work: "alternative A" vs "alternative B" on one ride each** (`demos/ways/church--work.html`). Both rides are work→church: 2026-06-07 10:21 (7 min) tagged `alternative A`, 2026-08-09 10:11 (8 min) tagged `alternative B`; no church→work ride exists. Two single-ride clusters cannot be ratified under D-015 / DATA-MODEL §8a. Work: hold both as provisional until more rides accrue (or Nathan rules by eye that both are real roads he uses); until then the way carries no coloured comparison | Navigation Engineer + Nathan | OPEN |
| B-64 | **Thin-evidence ways: provisional canonical routes and one-off corridors** flagged in Nathan's ways review. `demos/ways/fosh--home.html`: the ONLY home→fosh ride (2026-04-21 17:57) `takes a different corridor (0.13–0.17 overlap with the returns)`, and two of the three fosh→home rides contain long stops at work (2026-04-15 21:44, 57 min stop; 2026-06-04 20:49, 150 min stop) — are they fosh→home at all, or fosh→work + work→home? Only 2026-06-14 14:25 is a clean 22-min fosh→home. `demos/ways/church--fosh.html`: single ride 2026-06-14 12:21 tagged `only ride on this way — canonical for now`. `demos/ways/fosh--work.html`: canonical is a single ride 2025-10-08 15:26 (`more accurate`), the other superseded. `demos/ways/fosh--puttestraat.html`: 8 rides tagged `one route with variations; 1 is the most distinct (0.60–0.71)` — ride 2025-09-18 20:07 may be a variant, unruled. Work: mark these ways provisional in the catalog (no gates, no colour) until ride counts justify a D-015-style clustering pass; decide the stop-at-work rides' way membership per the landmark-visit rules in DATA-MODEL | Navigation Engineer + Product Owner | OPEN |
| B-65 | **Seed the live engine's route lock with the rider's RECORD-tab pick as a bias/hint, verified only AFTER the true GPS lock fires (a lock-then-verify mechanism, never a shortcut on lock timing).** The 2026-08-18 route-pick UI (WHICH ROUTE TODAY?) fixes the map preview but not the underlying miss Nathan reported: taking Evening B, the live map still shows the wrong route until gate 1, because `engine.ts` has no way to know the rider's declared pick and no unlock/re-detect path once locked. Evening A/B share their first stretch out of work, so biasing LOCK TIMING itself risks locking onto the wrong road when the rider deviates — violating §8a's own honesty invariant ("the ridden route wins, never a bad guess"). Needs its own fixtures in `tests/live_suite.ts`/`tests/live_colour_suite.ts` before it ships. | Race Engineer + Mobile Dev | DONE 2026-08-24 (cycle 024, WP-D1/WP-D2) — the anchored/pick/pickHonoured soft→verified→finalized lock state machine is exactly this mechanism, built full-catalog across all 20 routes; fixtures landed in `live_suite.ts`/`live_colour_suite.ts`. Adjudicated and ratified as **D-044** (2026-08-20 pick-bias ruling). **The doc-conflict half of this item is NOT resolved** — B-41 and `COLD-START.md` §10 still describe "auto-matched at route lock, don't ask upfront," contradicting `DATA-MODEL.md` §8a's "ask which route upfront" (what actually shipped); see B-152, filed this cycle, for the missing cross-reference |
| B-66 | **Regenerate `demos/mockup.html` for the cycle-020 race-mode layout** (map ≈ half screen on top, clock, sector blocks, status, PAUSE→RESUME/END at the bottom; DEMO tab follows the theme) — CLAUDE.md §6 requires it with any shipped design change; deferred on 2026-08-19 to get the build onto the commute | Designer | OPEN |
| B-67 | **Route-asset PNGs are stale for route B** — `EveningB.png` (PNG fallback rung) was rendered from ride 20260809-1900 and still shows the non-Beekstraat ending; MorningB has no PNG. `08_build_route_assets.py` picks "most recent B ride" — it must pin route 5 (20260520-2317) and learn MorningB (reversed) before a regen; needs Nathan's PC (Python + base crops) | Mobile Dev | OPEN — cosmetic while MapLibre is the live rung |
| B-68 | **GPX+ diagnostic export** (IDEAS §23, spec `product/superseded/GPX-PLUS-proposal.md`) — gate cleared once landmarks/ways existed (2026-08-18). A new `rides/<id>.events.jsonl` sidecar (button presses, route lock, gate crossings, GPS outages, stops, storage errors, relaunches) is derived at export time; the raw ride JSONL is never touched (D-023). `exportGpxPlus()` emits GPX 1.1 + a `<qf:...>` extensions block plus sparse per-fix `<qf:acc>` from GPS accuracy that was already recorded on disk but previously discarded on export. Standard `exportGpx()` proven byte-identical (regression-locked, `gpxplus_suite.ts` test e). Nathan then asked to fold GPX+ straight into the existing Rides-tab export flow rather than add a second button: `RidesScreen.tsx`'s one "Export GPX" button now calls `exportGpxPlus()` and reads "Export GPX+" | Backend Dev + Mobile Dev | DONE 2026-08-19 (cycle 021) — 11 files (+3 new), 145 tests (was 134): 142 pass / 0 fail / 3 skip unchanged; fresh Fable inspection PASS. See `cycles/cycle-021.md` |
| B-69 | **Two low-severity follow-ups from cycle 021's inspection, both deferred:** (1) wire PAUSE/RESUME button presses into `RecordScreen.tsx`'s onPress handlers via the already-exported `noteButtonPress()` — two one-line calls; (2) harden `gpxPlusExport.ts` — `escapeXml()` doesn't escape `"`, which the export uses inside XML attribute values (unreachable via normal app data today, only via a hand-edited sidecar), and a hand-corrupted sidecar line with a missing `track`/`t` field isn't validated before use and could throw during export | Mobile Dev + Backend Dev | DONE 2026-08-24 (cycle 024) — (1) closed early by WP-A2 (PAUSE/RESUME wired to `noteButtonPress()`); (2) closed by the cycle-023-remediation phase + WP-G (`escapeXml()` quote escaping; per-kind decoder field validation extended further by WP-G's own inspection blockers) |

## Next — once the concept holds still

| ID | Item | Owner role | Status |
|---|---|---|---|
| B-08 | Confirm tech stack (RN/Expo vs alternatives) | Mobile Dev | DONE → D-012 |
| B-09 | Route-matching approach: is this ride "the commute"? | Race Engineer | PART-DONE → D-015 offline; live auto-lock built + headless-verified (cycle 006, D-025) — device validation pending |
| B-10 | Data model for rides, sectors, sector times, benchmarks | Backend Dev | OPEN |
| B-11 | GPS quality plan: drift, urban canyons, background-execution limits | Mobile Dev | OPEN |
| B-12 | Test strategy — including replaying recorded traces as fixtures | QA | DONE — replay-fixture harness in effect (63 tests, cycle 006) |

## Later

| ID | Item | Owner role | Status |
|---|---|---|---|
| B-13 | Theoretical best lap (best of every sector combined), F1-style | Race Engineer | OPEN |
| B-14 | Season / championship framing over a longer horizon | Product Owner | OPEN |

---

**Rule:** an item may only be `IN CYCLE` if it appears on the current cycle's agenda. No more than three at once.
