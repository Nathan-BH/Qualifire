* Ride was uneventfull and clocked sector1-3 correctly

  * Except the last sector is annotated as "did not traverse" in the app for some reason
  * Took a slightly different route than the current reference so could explain ? is there somewhere an off-route metric logged at certain timepoints

    * I suspect I might have missed gate3 which could explain why it skipped sector 4 ?

