# BRIEF — `design/` folder: bring the code-driven mockups back in step with `virgin`

**Status: PLAN — nothing executed. No SVG or script was touched while writing this.**
**Open item:** Nathan asked (2026-09-08) for a review of `design/` and a plan for moving forward. Review is in `REVIEW-design-folder.md` (same folder); this is the plan. Add to `OPEN-ITEMS.md` under virgin-cycle5 (or the next cycle) when accepted.
**Written by:** Plan tier (Fable). Verified against the device tree 2026-09-08, branch `virgin`. Every `file:line` below was read today; re-grep before editing — the tree moves fast.
**Size:** medium overall. D0 is a chore; D1 is the one genuinely new drawing; D2 is a re-transcription pass; D3/D4 are optional and separable; D5.0 (added 2026-09-08, Nathan's Q8 answer) is a Sonnet collage-generation step; D5 itself is then a one-line answer for Nathan; D6 is five minutes of housekeeping.

---

## 0. The shape of the work

`design/canonical/` is regenerated by `design/make_screens.py`, which reads the app source. The right fix for staleness is therefore **always** "update the script, re-run, commit the output" — never hand-edit an SVG in `canonical/`. That rule holds for everything below.

Work packages, in the order they should run (each is one Sonnet Execute dispatch unless noted):

| WP | What | Blocks | Tier |
|---|---|---|---|
| **D0** | Make the generator run again (path renames, colour allow-list, vocabulary) and regenerate — *no drawing changes* | everything else | Sonnet, one commit |
| **D1** | Add RESULTS tab + RESULTS detail mockups | D0 | Sonnet (Plan already specified the layout below) |
| **D2** | Re-transcribe the screens that changed: ROUTES (post-WP-K/WP-3), RECORD ×4 (sport pills, WP-E tick retire, trail spans), `result` → `ride_detail` | D0 | Sonnet |
| **D3** | Optional: mockups for the remaining full-screen surfaces (catalog detail, gate editor, SETTINGS → SPORTS) | D0 | Sonnet; can be deferred |
| **D4** | Optional: empty-state variants (default seed mode) | D0 | Sonnet; can be deferred or folded into D1–D3 |
| **D5.0** | Comparison collages (PNG/JPEG grids) for both palette explorations — new, added 2026-09-08 | none — `drafts/` and `brand/palettes/` are independent of D0's generator | Sonnet |
| **D5** | The palette decision — Nathan's call, not ours | D5.0 (added 2026-09-08); blocks closure of `drafts/` | Nathan |
| **D6** | Housekeeping: `edited/` fixture, ChatGPT folder, `README.md` rewrite | none | coordinator / Haiku |

D0 → D1 → D2 is the minimum that makes the folder honest again. D3–D4 make it complete. D5.0 → D5 is a decision, now with the comparison step Nathan asked for first. D6 can happen any time.

**Acceptance rule for every WP that touches the script:** `python3 design/make_screens.py` exits 0, its built-in validator passes for every file, the run is byte-identical when run twice, and `git diff --stat design/canonical/` shows only the screens the WP claims to change.

---

## D0 — Make `make_screens.py` run again (chore, one commit)

**Why first:** the script currently crashes on its first file read (`REVIEW` §3.1). Nothing else can be verified until it runs.

### D0.1 Map asset path and key

`make_screens.py:325-329`, `load_route_asset()`:
- `app/assets/routes/routes.json` → `app/assets/ways/ways.json`
- `manifest["routes"][route_id]` → `manifest["ways"][way_id]`; rename the parameter `route_id` → `way_id`, default stays `"Morning"` (still present in the manifest).
- Confirm the per-way fields the projector uses (`path`, `gates`/`gateIdx`) are unchanged in shape — `ways.json` has `image, w, h, x0, y1, scale, offx, offy, gates, path, gateIdx, sourceRide`; `path` is what `make_projector()` reads.

### D0.2 Colour allow-list source

`make_screens.py:143-148`, `load_allowed_colors()`: replace `"app/src/ui/routeMapView.tsx"` with `"app/src/ui/wayMapView.tsx"` (the `CASING = '#14120C'` const is at `wayMapView.tsx:171`; it is *not* in `theme.ts`, so without this the validator rejects every map-bearing screen). Consider also making a missing allow-list file a **hard error** rather than `continue` — the silent skip is exactly what let this rot unnoticed.

### D0.3 Re-transcribe `THEMES`/`COLORS`

The `THEMES` dict is a hand transcription of `theme.ts` dated 2026-08-22. Diff it against today's `theme.ts` and re-transcribe any changed/added token. The validator will catch a colour used by the script that no longer exists in the source, but **not** a colour the app now uses that the script never draws (e.g. a new tier or accent). Read `theme.ts` end to end once.

### D0.4 Vocabulary (WP-3) in the script

Mechanical rename in comments, function names and drawn labels, following `GLOSSARY.md` (rewritten virgin-native in cycle 5): **route** = from→to path (parent), **way** = named variant (child). Concretely:
- `store/defaultRoute.ts` refs (`:627, :635`) → `store/defaultWay.ts`; re-read the renamed `routeLabel()`/`routeVariantLabel()` equivalents and re-mirror them exactly.
- `routeMapView.tsx` refs throughout (`:87, :89, :145, :427, :433, :442, :1085, :1088, :1175, :1231`) → `wayMapView.tsx`.
- Any drawn heading text: the ROUTES screen builder's section labels must match `RoutesScreen.tsx:48` (`YOUR PLACES`) and `:80` (`ROUTES`) verbatim — that is a D2 layout change, but the *label strings* can be fixed here if the builder is otherwise untouched.
- Do **not** rename the output files (`routes_*.svg` stays `routes_*.svg` — it is the ROUTES tab).

### D0.5 Tab bar label

The scrolling bar's sixth label `RESULT` → `RESULTS` (`App.tsx:215`: "WP-2 re-added RESULTS, in RESULT's old slot"). Every screen's bar changes by one word; expected in the diff.

### D0.6 Docstring

Update the module docstring (`:1-44`) to list the current source files and drop the cycle-024 narrative into a one-line history note. The docstring is the first thing the next agent reads.

**Expected diff:** every canonical file changes (tab label), map screens unchanged except the label. If any map geometry changes, stop and find out why — `ways.json` should carry the same `Morning` path.

---

## D1 — RESULTS tab and RESULTS detail (new drawings)

**Why:** an entire tab shipped (WP-2, 2026-09-06) with zero design representation. This is the one WP that adds new builders rather than fixing old ones.

### D1.1 `results_day.svg` / `results_night.svg` — the way list

Mirror `ResultsScreen.tsx` (sport-scoped, structure copied from `RidesScreen.tsx`'s outer shape, row styling copied from RidesScreen's row look). Read the file fresh; the shape at time of writing:
- bare sport-name badge line at top (same as ROUTES/RIDES, WP-1) — draw it with the fixture's default sport;
- header `RESULTS`;
- one row per way actually ridden, **most-ridden first**, full way name, ride count, all-time best lap; tap-through affordance (chevron or the same row cue RIDES uses);
- tab bar visible, scrolled so RESULTS is fully in view (it is the rightmost tab — the bar clips on the left, per the README's convention).
- Fixture data: reuse the same placeholder ways the RIDES mockup uses so the two screens tell one story. Times/counts are placeholders (README rule: "never a real ride result").

### D1.2 `results_detail_day.svg` / `results_detail_night.svg` — one way's history

Mirror `ResultsDetailScreen.tsx` (chrome modelled on `CatalogDetailScreen.tsx`: top bar `‹ BACK` / title / caption; bottom `BACK TO RESULTS` button; **full-screen, no tab bar**). Layout per its header (§3.10): way name → `LAST N RIDES` header + hint + the scatterplot → `ALL N RIDES · fastest first` board → `BACK TO RESULTS`.

The scatterplot (`resultsPlot.tsx`) is "plain Views from what `resultsPlotModel.ts` already computed": x = time (proportional, whole window fits, no panning), y = scored seconds **faster at the top**, a filled purple dot marks the PB, **no tier colouring anywhere on the board** (§3.2 of WP-2). The executor should read `resultsPlotModel.ts` and mirror its point placement rule in Python with the same fixture rides, so the SVG plot is a faithful projection, not a sketch — same discipline as the map projector. Board rows carry an accent bar on the selected ride (`selectedRideId`).

Rankings-off state (SETTINGS) collapses board rows — **do not draw** that variant; the default is rankings on.

### D1.3 README table rows

Add `results` and `results_detail` rows to the "Which screens are here" table (D6 rewrites that README anyway; if D1 lands first, add the rows and let D6 tidy).

**Expected diff:** four new files, nothing else changes.

---

## D2 — Re-transcribe screens that changed since 2026-08-31

One dispatch, one commit, but three independent sub-edits so an inspector can verify each.

### D2.1 ROUTES — post-WP-K, post-WP-3

`RoutesScreen.tsx` today: sport badge line; `YOUR PLACES` (all landmarks — places are shared across sports — with dormant treatment *and* a "not used by this sport" flag when no route of the active sport starts/ends there); `ROUTES` list; **tap-only** — "no row expands in place, no delete button ever renders here" (WP-K). The current `routes_*.svg` draws one way open with its map, gate markers and the hardcoded "6 ghost laps seeded · 4 sectors · START ~160 m in" caption (`make_screens.py:774`) — that card no longer exists on this screen; it moved to `CatalogDetailScreen` (D3). Remove the expanded card, draw the flat tap-only list, match the two headings verbatim.

Keep reading places from `catalog.seed.json` (D0 leaves `load_catalog()` alone) — it is the `shipped`-mode fixture and the only realistic data available; but change the README wording from "your real catalog data" to "the opt-in shipped seed, used as fixture data" (D6).

### D2.2 RECORD ×4 — sport pills, WP-E, trail spans

- **Sport-pill row** (`RecordScreen.tsx:1264-1273`): rendered on the setup phase only when `showSportPillRow(sportCount, settings.showSportPillOnRecord)` — i.e. 2+ sports AND the toggle on. Draw it **on `record_setup` only**, with two fixture sports (one active pill, `styles.pillOn`), and state in the README that the row is conditional. Confirm from the file whether it also renders in armed/running (at time of writing it is inside the setup branch).
- **Gate ticks (WP-E, `d7e925b`)**: ticks no longer recolour by tier — `gateColours={undefined}`. In `record_running`/`record_finished`, draw every tick in the unscored style (the dim yellow tick with black casing the README already describes) — and delete the README sentence claiming ticks turn tier-coloured.
- **Sector-coloured trail spans (WP-K, 2026-09-04)**: the ridden trail *between* gates is what now carries the tier signal. Read `wayMapView.tsx`'s trail layer (and `sectorTrailModel.ts`/`trailModel.ts`) for the exact colouring rule and stroke, and draw the ridden portion in `record_running`/`record_finished` with the fixture's per-sector tiers. This replaces, not supplements, the tick colouring.
- Cycle-2 WP-A (route-match trail visibility) and WP-M (two-finger rotation) may have changed the live map's look; the executor reads `wayMapView.tsx` fresh rather than trusting this brief.

### D2.3 `result` → `ride_detail`

`ResultScreen.tsx` is gone; its per-ride board is inside `RideDetailScreen.tsx` (WP-H: route + date, headline GATED lap, rank line, per-sector split table, "ON THIS ROUTE" personal bests, ridden trace on the map, Export GPX+ / Delete / Ignore-in-ranking / Make-reference actions; full-screen, `‹ BACK`, no tab bar). Rename the builder and the output files `result_*.svg` → `ride_detail_*.svg`, delete the old two files from `canonical/`, and re-transcribe the layout from the current file. This is the screen shown right after STOP, so it is arguably the second-most-seen screen in the app.

**Expected diff:** `routes_*`, all four `record_*`, `result_*` deleted, `ride_detail_*` added.

---

## D3 — Optional: the remaining full-screen surfaces

Three more screens exist with no mockup. They are lower traffic than D1/D2 and can wait, but each is one builder:

| File | Mirrors | Notes |
|---|---|---|
| `catalog_detail_*.svg` | `CatalogDetailScreen.tsx` (WP-K) | a place OR a way — pick the way variant (it has the map + real gate-set facts + "edit gates" entry); the old ROUTES expanded card's content lives here now |
| `gate_adjust_*.svg` | `GateAdjustScreen.tsx` (WP-J, cycle 2) + `gateAdjustCard.tsx` | full-screen gate editor with the "proper openmap render"; tap-then-nudge UI |
| `settings_*.svg` (update) | `settings.tsx:309-317` `SportsSection` | add the `SETTINGS → SPORTS` section (Seg-style switcher + the show-pills toggle) to the existing settings builder — this one is small and could be folded into D2 |

Recommendation: fold the settings update into D2; leave the two detail screens for a later cycle unless Nathan wants to redesign either.

---

## D4 — Optional: empty states

Since 2026-09-08 the default install is `EXPO_PUBLIC_SEED_MODE=empty`: no places, ways, routes, ghosts or bundled assets. ROUTES, RIDES, RESULTS and RECORD-setup each have an empty state a first-time user sees before anything else, and none is drawn. Given D-045 ruling 3 ("a stranger with a blank install must be able to use this app"), these are worth one mockup each — `<screen>_empty_day.svg` (day only is enough; the point is layout and copy, not palette). Source: each screen's zero-data branch (`RoutesScreen.tsx:45-47` "with zero sports, a plain nudge toward SETTINGS", etc.).

Recommendation: do this only if Nathan is actively working on first-run UX; otherwise park in `OPEN-ITEMS.md`.

---

## D5 — The palette decision (Nathan) — RULED 2026-09-08: option (a), both explorations closed

**Nathan's ruling, after seeing the D5.0 collages:** he doesn't like any of the alternatives,
and more importantly wants to stick with the app's original day/night colours permanently —
"if it exists in every colour, it loses its image." Both explorations (`product/brand/palettes/`
and `design/drafts/`) are discontinued. Recorded in `product/brand/README.md` and
`design/drafts/README.md`. Nothing is deleted — both folders stay as history, per option (a)'s
own text below. No code change (the app already ships palette A). This closes D5; nothing
further to decide here.

**Nathan's answer (Q8):** he can't form a visual opinion from the current material — the SVGs are
"nice for control... but not easy to look at" one at a time in Inkscape, slow to load individually.
**Ask, before re-asking for the decision:** for each candidate design, produce a PNG/JPEG collage
(a grid of that candidate's screens on one canvas, for a single glance) *in addition to* the SVGs,
which stay canonical. This is now **D5.0**, and it blocks D5 proper (the actual choice) rather than
being optional polish.

### D5.0 — Comparison collages (new sub-step, Sonnet, before D5 can close)
- **`design/drafts/` (pink / lightblue / green):** already 27 SVGs across the three palettes. Build
  one collage PNG/JPEG per palette (e.g. `design/drafts/collage_pink.png`, `_lightblue.png`,
  `_green.png`), each a grid of that palette's screens (the four `record_*` states + `demo_*` at
  minimum — whatever the full set is by then), captioned with the filename so Nathan can match a
  square back to a screen. A fourth collage showing the *current* shipped theme's equivalent
  screens side by side is worth adding too, so "keep current" has the same one-glance comparison
  as the alternatives.
- **`product/brand/palettes/`:** partially done already — `palette_A_signal.png` .. `palette_F_teal_whisper.png`
  are already single-composite images per candidate, and `<slug>/brandboard_<slug>.png` are already
  12-panel boards. Confirm which of `brandboard_{daylight,golden,navy,night}.png` corresponds to
  which of A(signal)/B(vibrant gold)/C(two-tone)/E(cool-gray) — the naming doesn't match 1:1 on
  sight — relabel or add a small caption image if it's ambiguous, rather than building new
  collages from scratch; this folder likely just needs a legend, not new renders.
- **Tooling note for the executor:** `rsvg-convert` is not installed on this box; ImageMagick's
  `convert` has an SVG delegate but it falls back to its own limited internal renderer without
  `rsvg-convert` present — verify visually that a test render matches the SVG (colours especially)
  before trusting a batch conversion; `pip install cairosvg` is a fallback with better fidelity if
  the ImageMagick path looks wrong. PIL/Pillow is already available for the grid compositing itself
  once each screen is a PNG.
- Output: nothing is deleted, nothing is decided — this only adds comparison images next to the
  existing SVGs, in both folders, so D5's actual question can be answered by looking rather than
  guessing.

**This is the only item in `design/` that cannot be closed by an agent.** State of play:

- `design/drafts/` — three complete chrome palettes, **pink / lightblue / green**, 27 day-mode SVGs, WCAG report passing (Gate A 33/33 chrome pairs ≥ AA; Gate B 42/42 verdict pairs not-worse than today's day theme). Chrome only — verdict colours (purple / green / yellow / no-data grey / rider blue / map casing) are held fixed in all three. Cycle 025 (2026-08-30), commit `ecd4f56`.
- `product/brand/palettes/` — an earlier, brand-level exploration (trials A–F, brandboards per survivor). `product/brand/README.md:19`: "palette decision OPEN — Nathan comparing `palettes/brandboard_*.png`".
- `app/src/ui/theme.ts` — unchanged by either. The app ships today's dark-first theme.

Two parallel explorations, both open, neither referencing the other. The options as they stand:

1. **Keep the current theme** — close both explorations as "considered, kept". Zero code work; `drafts/` stays as history.
2. **Adopt one of pink / lightblue / green as the day theme** — one WP: transcribe that `draft_palettes.py` token set into `theme.ts`'s day mode, re-run `make_screens.py` (the canonical set then shows it), delete `drafts/` (its job is done).
3. **Adopt a `product/brand` palette instead** — same shape of WP, but the drafts tooling would first need a fourth token set so the WCAG gates can be run on it (nothing in `product/brand` was contrast-checked the way `drafts/` was).
4. **Not now** — record "parked, revisit after first-run UX" in `OPEN-ITEMS.md` so it stops looking like an unanswered question every audit.

What blocks closure is only the choice. Recommendation on *process*, not taste: whichever way Nathan leans, the two explorations should be reconciled into one line in `OPEN-ITEMS.md` and one sentence in `product/brand/README.md`, so the next audit does not rediscover them separately. If he wants to compare on-device rather than on SVG, note that `draft_palettes.py`'s token sets are already in `theme.ts`'s shape — a throwaway `EXPO_PUBLIC_THEME=pink` build is a small ask.

~~Suggested question for `QUESTIONS-FOR-NATHAN.md`~~ — asked (Q8) and answered 2026-09-08: Nathan
wants the D5.0 collages first, then he'll pick. Don't re-ask the four-way choice until D5.0 has
landed and he's had a look.

---

## D6 — Housekeeping (no design judgment; coordinator or Haiku)

1. **`design/edited/record_armed_draft-pink.svg` — delete.** It is an agent-authored fixture (line 2: `<!-- hand-edit (simulated Inkscape re-save), cycle 025 palette draft pass -->`; the only change is accent `#C2185B → #D81B60`; no Inkscape metadata), left behind from proving the diff workflow. It is not Nathan's, it is not pending, and it can never match a canonical filename. Leave `PUT-EDITED-SVGS-HERE.txt`.
2. **`design/ChatGPT attempt at improved design/`** — keep in history, but: (a) move the four real phone screenshots (2026-08-29) to `design/reference/screenshots/` — they are the only ground-truth renders in the folder and should be findable; (b) either rename the remainder to `design/archive/chatgpt-reconstruction-2026-08-29/` or leave it and add one README line saying "external reconstruction, nothing adopted, superseded by canonical/". Nathan's folder, Nathan's call on the rename; the screenshot move is safe either way. Ask before moving anything Nathan committed himself (commits `576e245`, `63d50fd`, `ac8777a` are his).
3. **`design/README.md` rewrite** (after D0–D2 land, so it describes what is true): current screen table incl. `results`, `results_detail`, `ride_detail`; WP-3 vocabulary; gate-tick convention corrected to WP-E; "shipped-seed fixture, default install is empty" wording; a short section on `drafts/` and its status (D5 outcome); drop the cycle-024 narrative and the two "worth your quick call" items (the folder name/location was never contested and the workflow was never exercised — replace with a single line: "if you'd rather work differently, say so").
4. Add `design/__pycache__/` to `.gitignore` if it is not already covered (untracked today; harmless).

---

## 7. What this plan deliberately does not do

- It does not redesign anything. Every drawing change above is a transcription of what already ships. If Nathan wants to *change* a screen, the round-trip in `design/README.md` (edit in Inkscape → `edited/` → agreed change → app + script) is still the right channel — and after D0–D2 it will work again, because `canonical/` will once more match the phone.
- It does not pick a palette (D5).
- It does not move `design/` (the README's own open question 1); nobody has asked.
- It does not touch `product/brand/` beyond the one cross-reference in D5.

## 8. Order and sizing at a glance

| WP | Est. | Depends on |
|---|---|---|
| D0 generator repair | ~1 h Sonnet | — |
| D1 RESULTS + detail | ~2–3 h Sonnet | D0 |
| D2 ROUTES / RECORD / ride_detail (+ settings SPORTS) | ~2–3 h Sonnet | D0 |
| D3 catalog_detail, gate_adjust | ~2 h Sonnet, deferrable | D0 |
| D4 empty states | ~1 h Sonnet, deferrable | D0 |
| D5.0 comparison collages | ~30 min Sonnet | none |
| D5 palette | Nathan, one word | D5.0 |
| D6 housekeeping | ~20 min | D0–D2 for the README |
