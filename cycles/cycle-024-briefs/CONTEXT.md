# Cycle 024 — shared context pack (read this first)

## What this cycle is
Implement everything actionable from Nathan's notes files and the 19/20 Aug ride reviews that previous cycles have NOT already shipped. Nathan (the owner/manager) has ruled:

1. **Cycle 023 is still running on the same repo** (its scope: the 20 Aug notes — day-mode rendering bug on the race map, auto-pause/map-dim ideas + paused-time-in-result toggle, off-route-while-on-course investigation). **Cycle 024 must NOT write any repo file until 023 lands.** The current phase is ANALYSIS + BRIEF-WRITING ONLY. Every brief must end with an explicit "Files touched" list so the coordinator can order execution and check conflicts against 023's diff before executors start.
2. Scope: everything in the notes files — core app work AND the extras (housekeeping/docs, gate-field replay, SVG tab recompositions).
3. Reference promotions (19 Aug ride 1 → new Morning-B line "h>>w-w"; 19 Aug ride 3 → new station>>home wet route "s>>h-w") are approved **conditional on the analyst verifying the GPX quality supports it** (the 0819 review already found: ride 1's line is complete despite an 18-min recording hole only 7 m wide on the ground; ride 3 is genuinely a different road from the ratified preferred line, median ~200 m apart).
4. **NEW RULING (Nathan, 2026-08-20, resolves B-65's doc conflict):** the rider's RECORD-tab route pick **biases the engine's route lock as a hint, verified by GPS after the true lock fires** — if the rider actually rides a different road, the ridden route wins ("lock-then-verify", never a shortcut on lock timing). DATA-MODEL §8a's ask-upfront wins over B-41/COLD-START §10's pure auto-detect; those docs get a follow-up edit. Will be recorded as a decision in DECISIONS.md at bookkeeping time.

## Where the code is
A read-only snapshot of the repo's relevant files is staged under `/mnt/user-data/uploads/Qualifire/` (app/src/**, app/core/src/**, app/tests/**, App.tsx, demos/mockup.html, product docs, process docs, IDEAS.md, STATE.md, BACKLOG.md, cycle records 021–022, Nathan's notes + both ride-review folders, routes.json, catalog.seed.json, ride_curation.json, the two 0819 GPX files). This is a point-in-time copy; the live repo is on Nathan's Windows PC (mounted; do not write to it this phase). Need another file? Stage it read-only with mcp__remote-devices__device_stage_files (load via ToolSearch "select:mcp__remote-devices__device_stage_files,mcp__remote-devices__device_list_dir,mcp__remote-devices__device_bash" if deferred), device path prefix `C:\Users\natha\Claude personal projects\Qualifire\`. device_bash runs read-only commands on the PC (45 s cap, no network, never git — git in the mount leaves stale locks; git is Nathan's job).

## Hard constraints every brief must carry
- **Environment:** cloud sandbox has NO npm/PyPI/tile access. `app/tests/run.ts` runs pure via `node --experimental-strip-types tests/run.ts` without node_modules. `tsc --noEmit` and the full test suite run on Nathan's PC via device_bash (`cd $HOME/mnt/Qualifire/app && npx.cmd tsc --noEmit` equivalent — check README-dev.md for exact commands). Anything needing expo/react-native rebuilds, tile servers, or new packages → PowerShell script for Nathan (`powershell -ExecutionPolicy Bypass -File .\x.ps1`, ship a .cmd launcher).
- **Test regime:** last known suite state 145 tests, 142 pass / 0 fail / 3 benign skips (cycle 021) — cycle 023 may have moved this; executors re-baseline before touching anything. Every behaviour change lands with regression tests. No test may be weakened to pass.
- **Standing decisions:** D-023 raw ride JSONL is never rewritten (sidecars/derives only). D-042 raw time is the default (luck counts); PAUSE is an accidental-stop guard, recording/clock never stop. D-030 colour model, D-037 last-10 window, MIN_HISTORY 5. Honesty rules: no colour/verdict a sector hasn't earned; gates ahead stay uncoloured (but must be VISIBLE — dim theme-aware neutral, not near-black-on-near-black). Routes are ratified by Nathan, never auto-discovered. D-036: a new route may keep time, never compare, until 5 clean rides.
- **Nathan-facing language:** no bare B-NN/D-NN IDs in app UI or in anything Nathan reads standalone; plain language first, IDs in parentheses.
- **Stop-on-ambiguity:** executors must STOP and escalate (not guess) on any judgment call their brief doesn't pre-resolve. Brief-writers: pre-resolve every ambiguity you can see, and list the ones you can't as explicit NEEDS-NATHAN items.

## Post-analysis rulings from Nathan (2026-08-20, bind on executors)
- RECORD button uses the **theme accent** colour (no-red doctrine holds), not the mockup's red. Same flow/animation.
- Rider dot: **blue #2F7DE1 approved** (new palette hue, rider only); off-route as outline/badge change.
- Tower slot-in: Nathan indifferent ("whole tab updated per my notes anyway") → keep mockup-as-is default (reveal lives in Demo; RESULT = last ride + personal bests).
- Reference promotions CONFIRMED by analysis: PROMOTE both h>>w-w (ride 1 → MorningB ref) and s>>h-w (ride 3 → new StationHomeWet route). Numbers in WP-D1 brief.
- Execution order (Haiku triage + analyst sequencing): WP-H → WP-J (canonical set only; screens that change this cycle get re-emitted at the end) → WP-C → WP-D1 → WP-D2 → WP-D3 → WP-A1 → WP-A2 → WP-A3 → WP-G → WP-B → WP-E → WP-I last (bookkeeping). All AFTER cycle 023 lands; every executor re-stages live files and re-baselines tests first.
- WP-E/WP-G retry constants (30 m accuracy threshold, single post-settle retry, 30-fix arming window) accepted as engineering defaults — no Nathan ruling needed; inspector re-derives them.

## Work packages (assignments in each analyst's own prompt)
- **WP-A** Ride-history store + port the mockup redesign into the real app: RECORD three-phase flow (setup → armed → running) with launch animation forward/reversed, tab bar hidden while armed/running, RIDES tab as tappable/expandable ride history (route, date, lap, P-rank, sector splits vs ghost tower; export/delete demoted to detail view), RESULT tab as last-ride + personal-bests accordion. Spec = demos/mockup.html (cycle 022) + process/BETA-TESTERS.md findings. Underneath: the benchmark/ride-history store (B-28's other half, B-40 persistence already landed cycle 015).
- **WP-B** Free-ride "new" mode (Nathan's notes2, 20 Aug section): a "new" landmark option in the RECORD start/destination pick (work>>new, new>>home), map switches to gates-only view, gates auto-fire when crossed, sector times stored in a separate "free rides" category so fixed-route history stays clean.
- **WP-C** Route workbench browser page (demos/): per route — reference line on a real map (MapLibre/Leaflet per existing demos conventions), promote-a-ride-to-reference picker, gate handles draggable with live % + metres + Google Maps link readout, a percentage slider for naming points, Save writes a JSON file into the repo the team reads next cycle. Absorbs B-31 (gate eyeball: gate lines perpendicular at true half-width, crossing-point clouds from data/analysis/cache, 50 m arming window, σ_s labels) and B-42's promotion mechanism.
- **WP-D** All 19 catalog routes lock & score live (de-hardcode B-39: TRACK_IDS, FALLBACK_ROUTE, literal IDs, results.seed.json), pick-bias lock per the new ruling above (B-65, with fixtures in live_suite/live_colour_suite), reference promotions h>>w-w and s>>h-w (verify GPX first; build_track_ref.ts is the existing mechanism), cold-start gates for unratified routes (D-036 guards).
- **WP-E** Race-map render fixes: gate circles → thin line ticks perpendicular across the route in a dim theme-aware colour (visible in night mode, still verdict-free until scored); distinct rider-dot colour (off-route state as outline/badge); dotted route line ahead / solid behind the rider. HIGH conflict risk with cycle 023 (day-mode rendering fix) — brief must isolate touchpoints in routeMapView.tsx/routeMapGeo.ts/routeMapStyle.ts.
- **WP-G** GPX+ follow-ups: B-69 (wire PAUSE/RESUME noteButtonPress into RecordScreen; escapeXml quote-escaping; corrupted-sidecar validation), route-match diagnostics (accuracy at match time, threshold, deviation, retry once GPS settles — the 20 Aug ride-3 lock failure), elevation-spike outlier flag/filter, per-point or session-level on/off-route distance field. Check overlap with 023's off-route work before landing.
- **WP-H** Gate-field experiment, offline only: replay the archive (125 cached .npz rides in data/analysis/cache, index data/activity-index.csv) against a "free gates activated on crossing, no routes" model; report what it would have produced vs the route model. Evidence document, zero app changes. Python on Nathan's PC via script if the sandbox can't hold the data.
- **WP-I** Housekeeping & Nathan-facing docs: regenerate STATE.md (flagged overdue since cycle 016), product/ split into live vs proposals/ vs superseded/ (move, never delete), README per folder, fix stale BRAND.md (drop scarcity/162 m/three-tracks; colours-because-intuitive; night+day now, more themes later) and LAYOUT.md headers, root README folder list, plain-language "how the app is built" one-pager + glossary, standing plain-language status page for Nathan regenerated each cycle end.
- **WP-J** SVG recompositions of every app tab (+ race mode states) into a folder Nathan can edit in Inkscape and hand back: one labelled, grouped SVG per screen mirroring the real app's current rendered layout, plus a short round-trip convention doc (how Nathan's edited SVG gets read back into design changes).

## Brief output format (one .md per WP, self-contained — the executor sees ONLY the brief)
Write to /home/claude/cycle024/briefs/WP-X-<slug>.md with: goal in Nathan's words; exact current-state description with file paths + relevant line-level anchors from the snapshot; the precise change list; new/changed tests with expected counts; verification commands (sandbox-pure vs device_bash-on-PC); files-touched list; conflict-with-023 flags; pre-resolved ambiguities; NEEDS-NATHAN list (should be empty or tiny); rollback note. Assume the executor is Sonnet with no other context.
