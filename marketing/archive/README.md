# Archive

Superseded renders and composition sources, kept for reference. Nothing here is current —
check `marketing/README.md` and each composition's own `renders/` folder for what's live.
Naming: renders keep their original timestamped filename; composition sources are saved as
`<YYYY-MM-DD>_cycleNN_index.html` before a substantive rewrite.
