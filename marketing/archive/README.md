# Archive

Superseded renders and composition sources, kept for reference. Nothing here is current —
check `marketing/README.md` and each composition's own `renders/` folder for what's live.
Naming: renders keep their original timestamped filename; composition sources are saved as
`<YYYY-MM-DD>_cycleNN_index.html` before a substantive rewrite.

## pre-teaser-full-studios/ (added 2026-09-26, cycle 24)

The per-part folders and per-part renders of both `silent-studio/` and `audio-studio/`, retired when
the teaser became one single render (`silent-studio/teaser-full/`) instead of being assembled from
separate scene renders. Sub-paths mirror the original `silent-studio/…` / `audio-studio/…` layout.
Full path map in `../cycles/24_studio-folders-restructure/README.md`.
