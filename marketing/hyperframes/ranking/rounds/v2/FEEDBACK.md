# 04 — Ranking — Round v2

**Render file:** ranking_v2.mp4 — **not rendered yet.** Built 2026-09-10 without a shell on Nathan's PC;
Nathan renders it (`COMMANDS.md` → "The three product scenes") and copies the MP4 here.
**Duration (planned):** 15.2s at 30fps, 1920x1080, no audio. First HyperFrames render of this scene (v1 was a cut of the lost `tour` render).
**Source:** `../../index.html` (new, from scratch — `tour/index.html` no longer exists anywhere).

## What changed since v1
- Real map: OpenFreeMap dark basemap of the reference ride (Leuven, `qualifire-20260903-1828.gpx`, 5.8 km),
  captured once via `../../../_map/map-capture.html`, with the route/gates/rider drawn on top in the app's
  casing+core style and exact colours (route core #F5C542 / casing #14120C, rider #2F7DE1 — v1's #58B7FF was wrong).
- Continuity fixed: "Today" is 16:41.3 everywhere (fragment and tower). Times are realistic for the 5.8 km reference ride instead of v1's 4:05.3 (an 85 km/h ride would look wrong next to a real map).
- "P1 of 2" is now "P1 of 10" so the fragment and the ten-row tower describe the same history (flagged for Nathan).
- Rank numbers are fixed to the row slots; the rows move between them — the v1 "both rows read 1" glitch cannot occur.
- v1's black gap replaced by a sequential fade-out/fade-in (dimmed map stays visible throughout, so no black frame — not a true overlapping cross-dissolve).

## What you should see, in order (planned — verify against the render)
1. 0.3-2.75 — Map dims to ~30%; rank card slots in; Mon (17:02.8) at 1, Today (16:41.3, purple) slides in at 2 then overtakes (2.1-2.6); "P1 of 10" appears.
2. 2.75-5.0 — Caption "Now you know how you did."
3. 5.0-7.4 — Rank card fades out, timing tower fades in; ten rows fill top-down (5.9-7.6).
4. 8.25-15.2 — Caption "The more you ride it, the more you've got to race."; hold.

## Nathan's feedback
*(blank — write your notes below, overall or beat-by-beat)*

-
