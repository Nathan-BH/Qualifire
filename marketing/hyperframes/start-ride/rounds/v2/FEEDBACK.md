# 02 — Start Ride — Round v2

**Render file:** start-ride_v2.mp4 — **not rendered yet.** Built 2026-09-10 without a shell on Nathan's PC;
Nathan renders it (`COMMANDS.md` → "The three product scenes") and copies the MP4 here.
**Duration (planned):** 13.0s at 30fps, 1920x1080, no audio. First HyperFrames render of this scene (v1 was a cut of the lost `tour` render).
**Source:** `../../index.html` (new, from scratch — `tour/index.html` no longer exists anywhere).

## What changed since v1
- Real map: OpenFreeMap dark basemap of the reference ride (Leuven, `qualifire-20260903-1828.gpx`, 5.8 km),
  captured once via `../../../_map/map-capture.html`, with the route/gates/rider drawn on top in the app's
  casing+core style and exact colours (route core #F5C542 / casing #14120C, rider #2F7DE1 — v1's #58B7FF was wrong).
- START button is the app's RecordScreen button at 2x: 900x300, radius 48, "START" 80px/800/10px spacing, "the clock runs from here" beneath.
- Added the "Ride your normal route." caption Nathan asked for in idea-3 (v1 known issue).
- No black lead-in.

## What you should see, in order (planned — verify against the render)
1. 0.0-3.0 — Map with the rider at the start under a dim; START button fades in, is pressed (2.2s), fades out with the dim.
2. 3.0-4.3 — Rider waits at the start; caption "Ride your normal route." (3.6s).
3. 4.3-12.8 — Route draws behind the rider at constant speed.
4. 12.8-13.0 — Hold at the finish; caption fades.

## Nathan's feedback
*(blank — write your notes below, overall or beat-by-beat)*

-
