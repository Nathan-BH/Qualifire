# EXECUTOR-REPORT: cycle 19, teaser sound lanes

Executor: Sonnet 5, Execute tier. Date 2026-09-24. Brief: `BRIEF-teaser-sound-lanes.md`. (The brief calls this file EXEC-REPORT.md; the dispatch message asked for EXECUTOR-REPORT.md, so this is its name.)
Nothing was listened to. Everything below is measured (numbers) or looked at (screenshots, described honestly).

## Result in one paragraph
Phases A, B, C, D passed their checkpoints. Phase E STOPPED at step 3: `verify_default_mix.py` part (a) fails (max 642 LSB against a limit of 4). Cause found and measured: the 16-bit kit WAVs clip the samples that the mp3 decode puts above 0 dBFS. Per the brief's rule (any failure in this check is a stop) I did not change the kit format. The rest of Phase E (hygiene, av-align md5, git status, README, real-media browser run) is done. Tree is consistent: no file outside the two allowed folders was touched, av-align is byte-identical, no commit, no deletion.

## STOP: the question for the fresh Fable planner (verbatim facts)
Command: `cd marketing/audio-studio/tools/teaser-lanes && python3 verify_default_mix.py` (output copy: `cycles/19_teaser-sound-lanes/verify-default-mix.txt`):
```
clips mixed (4): logo@0.000, bed@10.300, bed@23.040, e5@24.300
mix length: 2099160 frames = 47.6000 s
(a) render [6.5, 32.8) vs ride_master_v2.wav: frames 1159830, max |diff| 642 LSB (at frame 588835), rms(diff) 3.2718 LSB, samples > 2 LSB: 1033  -> FAIL
   cross-correlation lag ours vs reference: 0 samples
(b) render [0, 6.5) vs soundtrack_v3.wav: frames 286650, max |diff| 2 LSB (at frame 30323), rms(diff) 0.4866 LSB, samples > 2 LSB: 0  -> ok
(c) render [32.8, 47.6): 652680 frames, non-zero samples: 0  -> ok
MIX FAIL
```
Diagnosis (independent scratch script in `$HOME/scratch-lanes/exp.py`, outside the repo): `decode_stereo` (float32) of `original.mp3` has peak 1.0435 with 583 samples above 1.0; `strings-model_other.mp3` peak 1.1309 with 29 samples above 1.0; `6stem_other.mp3` peak 1.0384 with 3 samples; all other sources stay below 1.0. `ffmpeg -c:a pcm_s16le` (brief section 4 step 4, "quantisation only") clips those samples: max |kit bed.wav - float decode| = 0.04351 at sample 421255; 0.0435 x gain 0.45 x 32767 = 641 LSB, which is the 642 seen. The ride master is unaffected because it mixes the float decode at gain 0.45 and only then quantises. Control experiment: float-decoded bed placed exactly as ride_tunetank.py + the kit's e5.wav vs ride_master_v2.wav: max 1 LSB, 0 samples > 2 LSB, rms 0.376. So the timing, gains, fades and the e5 lane are right; only the 16-bit transcode of overs is the problem.
Choices (not mine to rule): (A) write the kit WAVs as 32-bit float (`-c:a pcm_f32le`; kit wavs about 21 MB -> about 42 MB; `decodeAudioData` reads float WAV; prep_kit steps 4/7, `verify_default_mix.py` reader and the sample-count expectations keep working, the master-reproduction step 6 is unchanged); (B) keep 16-bit and accept the clipping with a documented looser tolerance for span (a) (max about 642 LSB on 1033 samples, i.e. up to 4.35 % on a few hundred bed peaks, more on a-other, which has 29 samples up to 1.13); (C) something else. Which one?
Not affected by the choice: everything in phases A, C, D, and 56 of the 58 real-kit browser checks (they do not depend on sample format).

## Interpretations and brief inconsistencies I resolved without stopping (please confirm or overrule)
1. `fmtSeconds` / `fmtNum`: the brief says "toFixed(3) first" and also expects 15.0465 -> "15.047", and the copy-list example prints `15.047` / `25.347`. In JavaScript `(15.0465).toFixed(3)` is "15.046". I implemented decimal round-half-up (`fmt3`) so the explicit expected outputs hold. All expected strings in the brief are met; the "toFixed" hint was not followed.
2. Default clip count: the brief says "exactly 19 clips" and (section 12.5) "the expected 20 lines". The enumerated content (logo 1, bed 2, six stems x 2, e5 1) is 16 clips; 17 lines with the header. I built the enumerated 16 (adding 3 invented clips would break the default-equals-today's-teaser rule). The copy-list test uses 17 lines.
3. Synthetic e2e "Reset -> the manifest's 8 clips": the synthetic manifest as specified has 7 clips (b-drums has one). Test asserts 7.
4. `logo` `source_len_s` is 8.4376 (372,096 samples / 44,100 rounded to 4 decimals, as the brief's own rule says) not the 8.4375 of the manifest example.
5. Overlapping clips in one lane header: section 5 defines `laneReadout` as the first clip's source time (which would show `src 14.000` for the bed at render 24.30), section 12.6 expects `src 1.260` for bed clip 2 at 24.30. Both instances of the bed are under the playhead there. I kept `laneReadout` exactly as specified and added `laneReadouts` (all clips, in order of `at`); the header shows them all: `src 14.000 | 1.260`. Alternatives: show only the last, or only the first.
6. Real-time play: when a clip is started inside its own fade-out, the brief's `setValueAtTime(c.gain, when + tFadeOutStart)` with `tFadeOutStart = 0` would jump the gain up; I skip that call in that case and ramp from the current level.
7. Additions to the core beyond section 5 (all additive, all tested): `fmt3`, `laneReadouts`, `nextClipId`, `kitRootOf`, `clamp`. `sourceTimeAt` uses a 1e-9 tolerance at both ends so a frame time such as 24.299999999999997 still counts as 24.3; `startParams` rounds to 0.1 ms.
8. Layout numbers: the left column is 605 px at 1440 (760 at 1920) including a 12 px padding, so the video panel is 581 px (736 px at 1920), not 605/760. The clip editor is 80 px tall with a title row plus two rows of fields, the frame numbers in 10 px type under the fields.
9. How-to under the editor: at 1440x810 the 8 lines do not fit (about 175 px free, about 250 needed), so the page shows only "press ? for the how-to", as the brief allows. They fit at 1440x900, 1920x990 and 1920x1080.
10. Video-file test artefact: cloud Chromium has no h264, so browser tests use a VP9 webm (synthetic 60 s and, for the real-kit run, 47.6 s / 1428 frames).
11. Landing files via `device_commit_files`: re-committing the same staged file name silently re-delivered the OLD content once (page landed as the 16 KB shell instead of 69 KB). I noticed by md5, re-staged under a new name and re-committed; every landed file now has the same md5 as the cloud copy (listed in phase E).
12. `kit/verify-default-mix.txt` was written by `tee` into the git-ignored kit folder, and copied to the cycle folder.

## Phase A: core and tests
Files: `tools/teaser-lanes/teaser-lanes.html` (core only at that point), `teaser-lanes.test.mjs`, `.gitignore` (`kit/` and `tests/out/`), cycle `shots/`.
Baseline before A (av-align): `0e08fc6c4a3871ff70c55ff23b3b12f7 README.md`, `a0faf5b80c72e418878dfb210a151eaf av-align.html`, `c0b5d4a579a67886ce7ead2eb076b57f av-align.test.mjs`. Baseline `git status --porcelain` showed unrelated pre-existing entries (RecordScreen.tsx, virgin-cycle13/14 files) and the new cycle folder; at the end only my two folders are listed (I did not touch the other entries; they are no longer listed).
Anchors re-verified with grep: av-align core block starts at line 360 (brief 362), `frameOf` 366, `shownFrameOf` 369, `seekTimeOfFrame` 370, `roundTenthMs` 371, `nudgeFrames` 374, `peakBins` 413, `ensureCtx` 605, `stopAll` 610, `startPlacement` 673, `tick` 694, `onFrame` 704, `loop` 711, `togglePlay` 719, `scrubPlay` 733, `loadVideo` 764, `stepTo` 776. All within the brief's tolerance.
Commands and results: `node teaser-lanes.test.mjs` in the cloud container (node v22.22.2) and in the VM (`cd $HOME/mnt/Qualifire/marketing/audio-studio/tools/teaser-lanes && node teaser-lanes.test.mjs`, node v22.23.2): `ALL PASS: 138/138 passed` at the checkpoint; the final file has `ALL PASS: 141/141 passed` (3 cases added for `laneReadouts`). All expected values are literals worked by hand, none computed with the function under test. Cases cover brief groups 1-18 (frames, clip end, two clocks, file offset, frameSlice, gainAt, startParams, audible, validateClip, parseManifest, buildState, formatClipList byte-for-byte, fmtNum/parseTimeEntry tables, layout and window functions, overlaps, serialize/restore, peakBins, file hygiene).
Not verified: nothing beyond the pure functions.

## Phase B: prep script and kit
Command (VM): `cd "$HOME/mnt/Qualifire/marketing/audio-studio/tools/teaser-lanes" && timeout 170 python3 prep_kit.py` (56 s). Ends `PREP OK`; report copied to `cycles/19_teaser-sound-lanes/prep-report.txt`.
Numbers from the run:
- video: md5 source = copy = `07f9c5b495519c97cdec3987decc027f`; ffprobe 1428 frames, 30/1, 47.600000 s.
- decoded sample counts: bed, a-strings, a-other, b-piano, b-drums, b-bass, b-other all 663,552 (15.0465 s); logo 372,096 (8.4376 s); e5 374,850 (8.5000 s). `original.mp3` md5 `a5ea27ca9bd0742beb78bf61bc0ae5fa`. Peaks (dBFS of the 16-bit WAV): logo 0.00, bed 0.00, a-strings -1.26, a-other 0.00, b-piano -0.87, b-drums -0.80, b-bass -12.99, b-other 0.00.
- E5: tau (ride clock) 17.8 / 19.81 / 21.65 / 23.51 / 25.38, peak 0.5025, slice from sample 784,980, pulses in the file at 0.0 / 2.01 / 3.85 / 5.71 / 7.58 s.
- master reproduction (kit chain vs `ride_master_v2.wav`): shapes equal (1,159,830 x 2); max |diff| 0 LSB, 0 samples > 1 LSB; derived e5 vs rendered e5 max 0.000031 (limit 1e-3); pulse rms ratios 113,189,177 / 113,189,177 / 113,189,177 / 18,054 / 9,413 (limit >= 100). PASS.
- alignment: self-test k = 100; stems vs bed (report only): a-strings 0 (corr 0.7842), a-other 0 (0.8913), b-piano 0 (0.5389), b-drums 0 (0.4782), b-bass 12 samples = 0.272 ms (0.4834), b-other 0 (0.8305); sum A lag 0 corr 0.9999; sum B lag 0 corr 0.9998 (corr = c[k]/sqrt(sum ref^2 sum x^2)). `file_offset_s` A = 0, B = 0.
- `kit/`: 9 wavs, `teaser_v9.mp4`, `manifest.json` (9 tracks, 16 clips), `prep-report.txt`; 33 MB. Idempotency: a second run gives identical md5 for all 9 wavs and the video; only `manifest.json` differs (its `made` timestamp).
Not verified: the 16-bit transcode is NOT sample-identical to the build scripts' float decode (see the STOP).

## Phase C: the page
Authored in the cloud, landed on the PC. Smoke run with the synthetic kit in Playwright: `ready · 5 lanes`, zero console errors. Screenshots looked at during C (scratchpad, not kept): 1440x900, 1440x810, 1920x1080. Defects seen and fixed then: lane labels cut ("strin...", "E5 pu..."), placeholder cut in the go-to box, playhead running below the last lane, stacked-mode sticky status bar not sticking, stale error text left in the status line after a good kit was opened.

## Phase D: synthetic browser verification
Commands (cloud): `node tests/synthetic-kit.mjs <dir>` then `node tests/e2e.mjs <dir> <outDir>` (Playwright 1.56.0, Chromium `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, `--autoplay-policy=no-user-gesture-required`). Result: `ALL PASS: 317/317 checks` (report: `cycles/19_teaser-sound-lanes/shots/e2e-report.txt`, and `tools/teaser-lanes/tests/out/report.txt`). Per viewport 1440x900, 1440x810, 1920x1080, 1920x990 (side) and 1000x800 (stacked): empty state; kit load in one action; 5 lanes; b-drums `missing: b-drums.wav`; step and go-to readouts (`0.100 s f3`, `2.000 s f60`, `2.333 s f70`, Home f0, End f1799); lane source clocks (logo 0.100 / bed dash at f3; bed 1.000 and e5 0.000 at f60); layout (no overlap between video, readout, transport, editor, how-to, ruler and lanes, everything inside the viewport, no page scroll, video 581/736 px wide and left of every lane, lane canvases all as wide as the ruler); M/S; the worked example (`+` at f1320, out 10, end `54.000`, a-strings src `3.500` at 47.5); the copy list equal to the literal 9 lines; duplicate/delete with the overlap count 0 -> 1 -> 0; zoom windows (`[43.75,44.25]`, frame ticks 46.8 px apart at 1440, click at the middle of frame 1323 lands on f1323); play 700 ms (video advanced >= 0.5 s, AudioContext `running`, resyncs 0 at every size); play into the end stops by itself; reload + reopen says "restored", Reset returns 7 clips; zero console and page errors. Robustness at 1440x900: garbage in go-to, `out` 0, gain 5 and gain -1 in the editor (a message each, field marked), `+` at the last frame, Delete with nothing selected, a folder without manifest.json, a manifest with an unknown track, a kit without its video: a visible message each, no uncaught error.
Test-design notes: readouts of `data-window`/`data-overlaps` update one animation frame after the action (tests wait two frames); a click exactly on a frame boundary can land one pixel to either side, so tests click the middle of a frame.
Screenshots (all in `cycles/19_teaser-sound-lanes/shots/`, all looked at with the Read tool):
- `empty-1440x900.png`, `empty-1440x810.png`, `empty-1920x1080.png`, `empty-1920x990.png`, `empty-1000x800.png`: dark page, video area on the left with the "Open kit folder" card and "or drop the folder here", transport greyed, empty lane area on the right. At 1440x810 the how-to is replaced by "press ? for the how-to". At 1000x800 the video is full width (976 px) and the page scrolls.
- `main-1440x900.png`: video 581 x 327 top left, large readout `47.500 s f1425`, transport, clip editor filled (a-strings, in 0.000 out 10.000 len 10.000 at 44.000 end 54.000, frame numbers under the fields), full 8-line how-to visible; right: ruler and 5 lanes at 96 px, selected block with a white border, red playhead through ruler and all lanes, strings A header `src 3.500`, b-drums lane amber `missing: b-drums.wav` with a dashed outline of its clip. Defect: the frame numbers under the editor fields are tiny.
- `main-1440x810.png`: same but how-to reduced to the one-liner (about 175 px of empty space under it).
- `main-1920x1080.png`, `main-1920x990.png`: video 736 x 414, lanes 96 px, everything legible, how-to complete; large empty area under 5 lanes (expected with 5 lanes).
- `main-1000x800.png`, `main-1000x800-fullpage.png`: stacked order video, readout, transport, editor, one-liner, ruler, lanes at 40 px; the fullpage capture has the status bar made static so it is not drawn over the editor.
- `zoom05-1440x900.png`: ruler with frame labels f1313 to f1326, current frame f1320 shaded, playhead at the start of the strings block (`in` tick), `src 0.000`.
- `help-1440x900.png`: help panel with the 8 lines and the key list over the right side; status line `ready · 5 lanes · restored your last clips and mutes` with the Reset button (the stale-error bug from an earlier run is gone).
- `real-main-1440x810.png`, `real-main-1440x900.png`, `real-main-1920x990.png`, `real-main-1920x1080.png`, `real-zoom05-1440x810.png`: the real kit (see phase E): 9 lanes with real waveforms (logo grey, bed blue, A teal, B violet, E5 amber), lanes 81/91/96/96 px, bed lanes' overlap 23.04 to 25.35 hatched, headers `src 14.000 | 1.260`, E5 `src 0.000`. Mutes in these shots are the state after the test swapped bed for A. At 0.5 s zoom the start of the E5 file sits on the playhead and its energy begins about 1 frame later (measured in `e5.wav`: first |x| > 0.01 at 26.85 ms = 0.81 frame; peak 0.5025 at 66.6 ms), which is the Salamander render's own onset, not a tool offset. Defect: in the hatched overlap the tiny "in"/"out" labels of the two clips run together ("inout").
Not verified in D: real h264 playback, real audio output (nothing is heard in headless), folder drag-and-drop (Playwright cannot simulate it).

## Phase E: real media on the PC and the rest
1. av-align md5 after everything: `0e08fc6c4a3871ff70c55ff23b3b12f7`, `a0faf5b80c72e418878dfb210a151eaf`, `c0b5d4a579a67886ce7ead2eb076b57f`: identical to the baseline.
2. Phase B numbers above.
3. `verify_default_mix.py`: STOP, see the top of this report. (b) and (c) pass.
4. `node teaser-lanes.test.mjs` in the VM: `ALL PASS: 141/141 passed`.
5. `python3 -c "import json;json.load(open('kit/manifest.json'))"` OK (9 tracks, 16 clips); `ls -la kit`: 9 wavs + `teaser_v9.mp4` + `manifest.json` + `prep-report.txt` (+ `verify-default-mix.txt`); `md5sum kit/teaser_v9.mp4` = `07f9c5b495519c97cdec3987decc027f`.
6. LF and BOM: every new file has 0 CR lines and no BOM (`.gitignore`, README.md, teaser-lanes.html, teaser-lanes.test.mjs, prep_kit.py, verify_default_mix.py, the three tests/*.mjs, manifest.json, both reports). The page has 0 matches for `https?://`, `<link`, `<script ... src`; 69,610 bytes. `GIT_OPTIONAL_LOCKS=0 git status --porcelain` at the end: only `?? marketing/audio-studio/tools/teaser-lanes/` and `?? marketing/cycles/19_teaser-sound-lanes/`. `kit/` and `tests/out/` are git-ignored by the new `.gitignore`. Landed md5s equal the cloud copies: teaser-lanes.html `a2764a4accaa43cb6c05a80f5a51cd2f`, test `68fa9599d6f10c1f5257dd332a23fcbd`, prep_kit.py `a3dad649ed2310f143f3d7e300d21d15`, verify_default_mix.py `2308330d45076564c47dd628ea23d5f1`, e2e.mjs `ca526524c6befe52a38967b8df40fc55`, e2e-real.mjs `53b67077ea61fc25768b2fffcefe0aed`, synthetic-kit.mjs `980a6924c22686ed7e8ee792910968ff`, README.md `7313fad1fb82adda71c27acf3526d7e8`.
7. Optional real-kit browser run: staging of the 9 wavs + manifest (about 21 MB) worked (staged md5s equal the PC's). A scratch copy with a synthetic 47.6 s webm and `video.file` patched: `node tests/e2e-real.mjs <dir> <outDir>` gives `ALL PASS: 58/58 checks` (`shots/e2e-real-report.txt`): 9 lanes; decoded sample counts equal the manifest's for all nine (`logo:372096 bed:663552 a-strings:663552 a-other:663552 b-piano:663552 b-drums:663552 b-bass:663552 b-other:663552 e5:374850`); load + decode of all files 518-606 ms in headless; lane heights 81 / 91 / 96 / 96 px at 1440x810 / 1440x900 / 1920x990 / 1920x1080; no page scroll, no overlap; at f729 = 24.300 s: bed `src 14.000 | 1.260`, every stem the same, e5 `src 0.000`, logo none; the default copy list equals the expected header + 16 lines written out from the brief's table; after swapping bed for A the list marks bed muted and A not; zoom 0.5 s window `[24.05,24.55]`; 1.5 s of play at 1440x810: video advanced 1.47 s, context running, resyncs 0; zero console errors.
8. README.md written (42 lines).
Not verified in E: the tool's audible output against the shipped mixes (blocked by the STOP), anything real-time by ear.

## What only Nathan can verify (his own Chrome/Edge on Windows)
- The page opens by double-click; the folder picker takes `kit/`; dropping the folder on the page works.
- The h264 `teaser_v9.mp4` decodes and plays in his browser (the cloud runs used a VP9 stand-in).
- Real-time video/audio feel (about one frame on wired output), the sound heard while stepping, Bluetooth latency.
- Legibility of the fonts and the small text (frame numbers under the editor fields, in/out labels) on his Windows screens.
- That the default state sounds like today's teaser.

## Readout
| tier | model | tokens | outcome |
|---|---|---|---|
| Execute | Sonnet 5 | not visible to the executor | A, B, C, D passed; E stopped at `verify_default_mix.py` (a): 16-bit kit WAVs clip decode overs, max 642 LSB; question above |

## Ruling 1 follow-up (2026-09-24)
Source: `RULINGS-1.md` (fix instructions 1-9) and the 18 places in the brief tagged "Ruling 1". Ruling: the kit WAVs become 32-bit float; all 12 resolutions above confirmed; two UI fixes. Nothing was listened to. All commands ran as before (authoring in the cloud, landing with `device_commit_files` under fresh staged names, md5 compared on the PC, runs in the VM).

Step 1, `prep_kit.py` (float kit): added `write_wav_f32` (plain 44-byte header, tag 3, no `fact` chunk) and `read_wav_f32`, step 4 = `rt.decode_stereo` -> `write_wav_f32` -> `read_wav_f32` with the read-back equality Stop, `to_mono_f` / `peak_dbfs` on floats, e5 written and read back the same way (read-back check 1e-6), `peak` on all 9 manifest tracks, `how_made.decode` / `how_made.wav` (numbers written from the run), the new `notes` line; `ff_transcode` deleted (`wave` and `read_wav_int16` kept for `ride_master_v2.wav`). `cd marketing/audio-studio/tools/teaser-lanes && timeout 170 python3 prep_kit.py` (27 s): `PREP OK`. Sample counts 663,552 x 7, logo 372,096, e5 374,850. Peaks and overs: bed +0.37 dBFS over 583, a-other +1.07 over 29, b-other +0.33 over 3, logo -0.00 / a-strings -1.26 / b-piano -0.87 / b-drums -0.80 / b-bass -12.99 over 0. Step 6: max 0 LSB, 0 samples > 1 LSB, derived e5 diff 0.000031. Step 7 lags 0 everywhere, b-bass 12 samples. `ls -l kit/*.wav`: 5,308,460 bytes x 7, logo 2,976,812, e5 2,998,844. `xxd -l 44 kit/bed.wav`: `0300 0200 44ac 0000` at offset 0x14. Second run: identical md5 for all 9 wavs and the video; `manifest.json` differs only in `made`. Wav md5s: bed `24f986c462d9060d09d40bc8c53694c0`, a-other `a6927b525967ed135a169d738ba01dd5`, e5 `89052834efa4c1bdb7172e83a75ca70b`, logo `0555e5fc997e7532a38726afb3bcac7b`. `kit/prep-report.txt` copied to the cycle folder (md5 `78b0448f9033d9867e5ab9305182c63f` both).

Step 2, `verify_default_mix.py`: kit files now read by a copied `read_wav_f32`, `read_wav_int16` kept for `ride_master_v2.wav` and `soundtrack_v3.wav`, no `/32767` on kit data, header line added. `python3 verify_default_mix.py | tee kit/verify-default-mix.txt` (exit 0):
```
kit format: float32 (max |sample| over the kit: 1.1309)
clips mixed (4): logo@0.000, bed@10.300, bed@23.040, e5@24.300
mix length: 2099160 frames = 47.6000 s
(a) render [6.5, 32.8) vs ride_master_v2.wav: frames 1159830, max |diff| 1 LSB (at frame 816792), rms(diff) 0.0031 LSB, samples > 2 LSB: 0  -> ok
(b) render [0, 6.5) vs soundtrack_v3.wav: frames 286650, max |diff| 0 LSB (at frame 0), rms(diff) 0.0000 LSB, samples > 2 LSB: 0  -> ok
(c) render [32.8, 47.6): 652680 frames, non-zero samples: 0  -> ok
MIX OK
```
(a) is within the expected 1 LSB (limits unchanged: 4 / 1.5). Copied to the cycle folder (md5 `328c3291be0b45888e96af6163e16458` both). This replaces the failing run shown in the STOP section above.

Steps 3-5, page: `R.el.dataset.peak = pkAll.toFixed(4)` from the max |sample| over every channel (before the mono averaging); `out` label now `textBaseline = "bottom"` at `(x1 - 19, h - 2)` with its tick `fillRect(x1 - 2, h - 7, 1, 7)`, `in` unchanged at the top; editor CSS `.fr` 11px/11px, `.cell` height 28, `#clip-editor` height 84 (kept at 84: no layout assertion failed at any of the five sizes, so no revert to 80). Page 69,794 bytes.

Step 6, tests: `parseManifest` case for `peak` and `how_made.decode/wav` added; `node teaser-lanes.test.mjs` cloud and VM: `ALL PASS: 142/142 passed`. Synthetic kit (`node tests/synthetic-kit.mjs <dir>` then `node tests/e2e.mjs <dir> <out>`): `ALL PASS: 322/322 checks` (317 + 1 per viewport for "bed data-peak exists and is <= 1.0000", 5 viewports), zero console and page errors. Real kit (float wavs + manifest re-staged, 42.5 MB, md5 of the 9 wavs equal to the PC's; scratch copy with the synthetic webm and `video.file` patched): `node tests/e2e-real.mjs <dir> <out>`: `ALL PASS: 69/69 checks` (58 + 9 per-lane `data-peak` vs manifest `peak` within 0.0005 + 2 explicit: bed >= 1.04, a-other >= 1.10; done once, at the first viewport). `data-samples` unchanged for all nine. `data-peak`: logo 0.9997, bed 1.0435, a-strings 0.8648, a-other 1.1309, b-piano 0.9046, b-drums 0.9119, b-bass 0.2241, b-other 1.0384, e5 0.5025 (so Chromium decodes the float WAV unclipped). Load + decode of the nine wavs and the video: 948 / 642 / 756 / 655 ms in headless (under 1 s, the first run only just). Reports and all 18 screenshots re-taken and copied over the old ones in `cycles/19_teaser-sound-lanes/shots/` (`e2e-report.txt`, `e2e-real-report.txt`).

Screenshots looked at again (Read tool): `real-main-1440x810.png`, `main-1440x900.png` (plus 4x/2x crops of the overlap and the editor), `real-main-1920x1080.png`, `zoom05-1440x900.png`.
- In the hatched overlap of every bed-family lane (render 23.04 to 25.35) the second clip's `in` sits at the top left of the hatch and the first clip's `out` at the bottom right of the hatch: two separate labels, no more "inout". Remaining blemish: the first clip's `out` is drawn over the hatch lines at 1440 px width, so it is slightly dimmer and crossed by the diagonal lines; still readable.
- Editor at 1440x900 (a-strings selected): the frame numbers under in / out / len / at / end / fade in / fade out (`f0`, `f300`, `f300`, `f1320`, `f1620`, `f0`) are now visibly larger than before (11 px per the CSS; I did not measure pixels in the image), not clipped, no overlap with the next row or the how-to. At 1440x810 the editor holds its hint text (no clip selected) and the how-to is still the one-liner; the layout assertions (no overlap, no scroll) pass there.
- `real-main-1920x1080.png`: 9 lanes, headers `src 14.000 | 1.260`, tags at the block corners; nothing overlapping. `zoom05-1440x900.png`: unchanged from before apart from the `in` label.

Step 7, landing and checks (md5 on the PC equals the cloud copy for every file): teaser-lanes.html `cd0bf7d96d98bf2ebd12935a0cc42ba8`, teaser-lanes.test.mjs `7d13cb5d1f84ecfddadc92f82365d5f8`, README.md `5adb7d0fdcf92ac1d75858f7e67adbd7`, tests/e2e.mjs `39e4d5270d1daad5b539717b0cabb8b4`, tests/e2e-real.mjs `c419359208b4e9d5120e140ab9770ead`, prep_kit.py `9901b187e5b442d426506ee3ecacdb9c`, verify_default_mix.py `fd823bc7ad3d098d12f234f3e4335fd3` (tests/synthetic-kit.mjs unchanged). VM: `node teaser-lanes.test.mjs` 142/142; `manifest.json` parses (9 tracks, 16 clips); `ls -la kit` = 13 entries (9 wavs, `teaser_v9.mp4`, `manifest.json`, `prep-report.txt`, `verify-default-mix.txt`); `md5sum kit/teaser_v9.mp4` = `07f9c5b495519c97cdec3987decc027f`; 0 CR and no BOM in every changed file, both reports and the manifest; the page has 0 matches for `https?://`; av-align md5s still `0e08fc6c4a3871ff70c55ff23b3b12f7`, `a0faf5b80c72e418878dfb210a151eaf`, `c0b5d4a579a67886ce7ead2eb076b57f`; `GIT_OPTIONAL_LOCKS=0 git status --porcelain`: only `?? marketing/audio-studio/tools/teaser-lanes/` and `?? marketing/cycles/19_teaser-sound-lanes/`. No commit, no deletion.

Step 8: README.md updated (float kit bullet replacing the "(a) currently FAILS" bullet, "decodes ... to 32-bit float WAV", "about 55 MB", use-line 4 "left to right in start order", check counts 142 / 322 / 69).

Not verified (unchanged): audible output of any kind, real h264 playback, real-time feel, folder drag-and-drop, the tool in Nathan's own Chrome/Edge. The tool has been checked to decode the float kit unclipped (`data-peak`) and the offline mix of the default clips has been checked against the shipped soundtracks by measurement; whether it sounds the same has not been heard.

| tier | model | tokens | outcome |
|---|---|---|---|
| Execute (Ruling 1 follow-up) | Sonnet 5 | not visible to the executor | steps 1-9 done, no new stop: prep OK (float kit), verify (a) 1 LSB / (b) 0 / (c) 0, MIX OK, core 142/142, synthetic e2e 322/322, real-kit e2e 69/69, av-align unchanged, git status only the two folders |
