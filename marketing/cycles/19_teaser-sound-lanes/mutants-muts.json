[
["M1 sourceTimeAt +1 frame", "return c.in + Math.max(0, tR - c.at);", "return c.in + Math.max(0, tR - c.at) + 1/30;"],
["M2 sourceTimeAt wrong sign", "return c.in + Math.max(0, tR - c.at);", "return c.in + Math.max(0, c.at - tR);"],
["M2b sourceTimeAt ignores in", "return c.in + Math.max(0, tR - c.at);", "return Math.max(0, tR - c.at);"],
["M3 mute does nothing (audible)", "m.set(l.id, anySolo ? !!l.solo : !l.muted);", "m.set(l.id, anySolo ? !!l.solo : true);"],
["M3b solo ignored", "m.set(l.id, anySolo ? !!l.solo : !l.muted);", "m.set(l.id, !l.muted);"],
["M4 copy list swaps in/out", "\" s -> render \" + fmtSeconds(c.at)", "\" s -> render \" + fmtSeconds(c.at)"],
["M4b copy list in/out swapped", "fmtSeconds(c.in) + \"-\" + fmtSeconds(c.out)", "fmtSeconds(c.out) + \"-\" + fmtSeconds(c.in)"],
["M5 frameSlice ignores in", "fileStart: fileTimeOfSource(track, c.in + (s0 - c.at))", "fileStart: fileTimeOfSource(track, (s0 - c.at))"],
["M5b frameSlice off by one frame", "const end = clipEnd(c), s0 = Math.max(n / fps, c.at), s1 = Math.min((n + 1) / fps, end)", "const end = clipEnd(c), s0 = Math.max((n+1) / fps, c.at), s1 = Math.min((n + 2) / fps, end)"],
["M6 clipEnd = at + out", "const clipEnd = c => roundTenthMs(c.at + clipLen(c));", "const clipEnd = c => roundTenthMs(c.at + c.out);"],
["M7 startParams ignores in", "fileOffset: roundTenthMs(fileTimeOfSource(track, c.in + rel))", "fileOffset: roundTenthMs(fileTimeOfSource(track, rel))"],
["M8 fade-out direction", "fo = c.fade_out > 0 ? Math.min(1, (end - tR) / c.fade_out) : 1", "fo = c.fade_out > 0 ? Math.min(1, (tR - c.at) / c.fade_out) : 1"],
["M9 validate allows out==in", "if (clip.out <= clip.in) errors.push", "if (clip.out < clip.in) errors.push"],
["M10 file offset sign", "const fileTimeOfSource = (track, tS) => tS + ", "const fileTimeOfSource = (track, tS) => tS - "],
["M11 seekTimeOfFrame n+1", "const seekTimeOfFrame = (n, fps) => (n + 0.5) / fps;", "const seekTimeOfFrame = (n, fps) => (n + 1.0) / fps;"],
["M12 list not sorted by at", ".sort((a, b) => a.c.at - b.c.at || a.i - b.i);", ";"],
["M13 fmt3 rounds half down", "r = Math.floor((k + 5) / 10)", "r = Math.floor((k + 4) / 10)"],
["M14 list omits muted flag", "if (!aud.get(lane.id)) parts.push(\"muted\");", ""],
["M15 validate at>=duration allowed", "clip.at >= video.duration_s) errors.push", "clip.at > video.duration_s) errors.push"],
["M16 startParams delay sign", "const delay = rel < 0 ? roundTenthMs(-rel) : 0;", "const delay = rel < 0 ? roundTenthMs(rel) : 0;"],
["M17 clipsUnder unsorted desc", ".sort((a, b) => a.at - b.at);\n  function laneReadout", ".sort((a, b) => b.at - a.at);\n  function laneReadout"],
["M18 windowFor not clamped", "const t0 = clamp(0, tPlay - w / 2, dur - w);", "const t0 = tPlay - w / 2;"],
["A1 scrub ignores mute", "if (!lane || !R || !R.buf || !aud.get(lane.id)) continue;", "if (!lane || !R || !R.buf) continue;"],
["A2 lane gain always 1 (mute does nothing)", "if (R && R.gain) R.gain.gain.value = a.get(l.id) ? 1 : 0;", "if (R && R.gain) R.gain.gain.value = 1;"],
["A3 play starts file at 0", "node.start(when, p.fileOffset, p.dur);", "node.start(when, 0, p.dur);"],
["A4 scrub plays file from 0", "node.start(w, s.fileStart, s.len);", "node.start(w, 0, s.len);"],
["A5 scrub plays one frame late", "const s = C.frameSlice(c, lane, n, S.fps);", "const s = C.frameSlice(c, lane, n + 1, S.fps);"],
["A6 video seeks one frame late", "video.currentTime = C.seekTimeOfFrame(n, S.fps);", "video.currentTime = C.seekTimeOfFrame(n + 1, S.fps);"],
["A7 lane src readout +1 frame", "const vs = C.laneReadouts(S.st.clips, l.id, t + 1e-9).map(C.fmtSeconds);", "const vs = C.laneReadouts(S.st.clips, l.id, t + 1 / 30).map(C.fmtSeconds);"],
["A8 waveform drawn ignoring in", "const fA = C.fileTimeOfSource(l, c.in + (tA - c.at)) * SR / bw, fB = C.fileTimeOfSource(l, c.in + (tB - c.at)) * SR / bw;", "const fA = C.fileTimeOfSource(l, (tA - c.at)) * SR / bw, fB = C.fileTimeOfSource(l, (tB - c.at)) * SR / bw;"],
["A9 len field sets out = len", "else if (f === \"len\") next.out = C.roundTenthMs(c.in + v);", "else if (f === \"len\") next.out = v;"],
["A10 play: fade-out ramp missing", "g.gain.linearRampToValueAtTime(0, when + p.tEnd);", ""],
["A11 play skips muted lanes' gain but plays solo wrong", "for (const c of S.st.clips) { const l = laneOf(c.track); if (l) startClip(c, l, m); }", "for (const c of S.st.clips.slice(1)) { const l = laneOf(c.track); if (l) startClip(c, l, m); }"],
["A12 resync never", "if (Math.abs(drift) > 0.025) {", "if (false) {"],
["A13 duplicate keeps old at", "const r = C.validateClip(Object.assign({}, c, { at }), laneOf(c.track), S.manifest.video);", "const r = C.validateClip(Object.assign({}, c), laneOf(c.track), S.manifest.video);"],
["A14 addClip at playhead+1 frame", "const at = C.roundTenthMs(clamp(0, curFrame(), S.total) / S.fps);\n    if (at >= S.manifest.video.duration_s) { say(\"playhead is at the end\", \"err\"); return; }", "const at = C.roundTenthMs(clamp(0, curFrame() + 1, S.total) / S.fps);\n    if (at >= S.manifest.video.duration_s) { say(\"playhead is at the end\", \"err\"); return; }"]
]
